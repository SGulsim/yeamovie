import { HeroBanner, ContentCatalog, VideoFilters } from '@/widgets';

const HomePage = () => {
	return (
		<main>
			<HeroBanner />
			<ContentCatalog />
			<VideoFilters />
		</main>
	);
};

export default HomePage;
