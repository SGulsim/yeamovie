# PROJECT CONTENT COLLECTION
# Generated: 2025-11-29T17:04:39.948Z
# Total files: 116

================================================================================

FILE: collect-project.mjs
────────────────────────────────────────────────────────────────────────────────

import fs from 'fs';
import path from 'path';
import { fileURLToPath } from 'url';

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);

const PROJECT_ROOT = path.resolve(__dirname);
const OUTPUT_FILE = path.join(__dirname, 'project-content.txt');


const IGNORE_PATTERNS = [
	'node_modules',
	'dist',
	'build',
	'.git',
	'.vscode',
	'.idea',
	'coverage',
	'.tmp',
	'package-lock.json',
	'yarn.lock',
	'pnpm-lock.yaml',
	'project-content.txt',
];


const INCLUDED_EXTENSIONS = [
	'.ts',
	'.tsx',
	'.js',
	'.jsx',
	'.json',
	'.html',
	'.scss',
	'.css',
	'.md',
	'.txt',
	'.mjs',
	'.cjs',
];


function removeComments(content, filePath) {
	const ext = path.extname(filePath);

	switch (ext) {
		case '.ts':
		case '.tsx':
		case '.js':
		case '.jsx':
		case '.mjs':
		case '.cjs':
			
			return content
				.replace(/\/\*[\s\S]*?\*\
				.replace(/\/\/.*$/gm, '')
				.trim();

		case '.scss':
		case '.css':
			
			return content
				.replace(/\/\*[\s\S]*?\*\
				.replace(/\/\/.*$/gm, '')
				.trim();

		case '.html':
			
			return content.replace(/<!--[\s\S]*?-->/g, '').trim();

		case '.json':
			
			return content
				.replace(/\/\*[\s\S]*?\*\
				.replace(/\/\/.*$/gm, '')
				.trim();

		default:
			return content.trim();
	}
}


function shouldIgnore(filePath) {
	const relativePath = path.relative(PROJECT_ROOT, filePath);
	const name = path.basename(filePath);

	return IGNORE_PATTERNS.some(
		pattern => relativePath.includes(pattern) || name === pattern
	);
}


function shouldInclude(filePath) {
	if (fs.statSync(filePath).isDirectory()) return false;

	const ext = path.extname(filePath);
	return INCLUDED_EXTENSIONS.includes(ext);
}


function readDirectory(dirPath, fileList = []) {
	const items = fs.readdirSync(dirPath);

	for (const item of items) {
		const fullPath = path.join(dirPath, item);

		if (shouldIgnore(fullPath)) continue;

		if (fs.statSync(fullPath).isDirectory()) {
			readDirectory(fullPath, fileList);
		} else if (shouldInclude(fullPath)) {
			fileList.push(fullPath);
		}
	}

	return fileList;
}


function collectProject() {
	console.log('🔍 Сбор файлов проекта...');

	const allFiles = readDirectory(PROJECT_ROOT);
	console.log(`📁 Найдено файлов: ${allFiles.length}`);

	let outputContent = `# PROJECT CONTENT COLLECTION\n`;
	outputContent += `# Generated: ${new Date().toISOString()}\n`;
	outputContent += `# Total files: ${allFiles.length}\n\n`;
	outputContent += '='.repeat(80) + '\n\n';

	for (const filePath of allFiles) {
		try {
			const relativePath = path.relative(PROJECT_ROOT, filePath);
			const content = fs.readFileSync(filePath, 'utf8');
			const contentWithoutComments = removeComments(content, filePath);

			outputContent += `FILE: ${relativePath}\n`;
			outputContent += '─'.repeat(80) + '\n\n';
			outputContent += contentWithoutComments + '\n\n';
			outputContent += '─'.repeat(80) + '\n\n';

			console.log(`✅ Обработан: ${relativePath}`);
		} catch (error) {
			console.log(`❌ Ошибка при обработке: ${filePath}`, error.message);
		}
	}

	outputContent += '='.repeat(80) + '\n';
	outputContent += '# END OF PROJECT CONTENT\n';

	
	fs.writeFileSync(OUTPUT_FILE, outputContent, 'utf8');
	console.log(`\n📄 Проект собран в: ${OUTPUT_FILE}`);
	console.log(
		`📊 Размер файла: ${(
			Buffer.byteLength(outputContent, 'utf8') /
			1024 /
			1024
		).toFixed(2)} MB`
	);
}


collectProject();

────────────────────────────────────────────────────────────────────────────────

FILE: eslint.config.js
────────────────────────────────────────────────────────────────────────────────

import js from '@eslint/js'
import globals from 'globals'
import reactHooks from 'eslint-plugin-react-hooks'
import reactRefresh from 'eslint-plugin-react-refresh'
import tseslint from 'typescript-eslint'
import { defineConfig, globalIgnores } from 'eslint/config'

export default defineConfig([
  globalIgnores(['dist']),
  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      js.configs.recommended,
      tseslint.configs.recommended,
      reactHooks.configs.flat.recommended,
      reactRefresh.configs.vite,
    ],
    languageOptions: {
      ecmaVersion: 2020,
      globals: globals.browser,
    },
  },
])

────────────────────────────────────────────────────────────────────────────────

FILE: index.html
────────────────────────────────────────────────────────────────────────────────

<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<link rel="icon" type="image/svg+xml" href="/vite.svg" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>yeamovie</title>
	</head>
	<body>
		<div id="root"></div>
		<script type="module" src="/src/app/appEntry.tsx"></script>
	</body>
</html>

────────────────────────────────────────────────────────────────────────────────

FILE: package.json
────────────────────────────────────────────────────────────────────────────────

{
	"name": "yeamovie",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "tsc -b && vite build",
		"lint": "eslint .",
		"preview": "vite preview",
		"collect": "node collect-project.mjs"
	},
	"dependencies": {
		"@reduxjs/toolkit": "^2.10.1",
		"react": "^19.2.0",
		"react-dom": "^19.2.0",
		"react-redux": "^9.2.0",
		"react-router": "^7.9.6",
		"react-router-dom": "^7.9.6"
	},
	"devDependencies": {
		"@eslint/js": "^9.39.1",
		"@types/node": "^24.10.1",
		"@types/react": "^19.2.2",
		"@types/react-dom": "^19.2.2",
		"@vitejs/plugin-react": "^5.1.0",
		"eslint": "^9.39.1",
		"eslint-plugin-react-hooks": "^7.0.1",
		"eslint-plugin-react-refresh": "^0.4.24",
		"globals": "^16.5.0",
		"sass-embedded": "^1.93.3",
		"scss-reset": "^1.4.6",
		"typescript": "~5.9.3",
		"typescript-eslint": "^8.46.3",
		"vite": "^7.2.2"
	}
}

────────────────────────────────────────────────────────────────────────────────

FILE: README.md
────────────────────────────────────────────────────────────────────────────────



────────────────────────────────────────────────────────────────────────────────

FILE: src\app\appEntry.tsx
────────────────────────────────────────────────────────────────────────────────

import './index.scss';
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import { RouterProvider } from 'react-router-dom';
import { Provider } from 'react-redux';
import { store } from './appStore';
import { appRouter } from './appRouter';
import { ErrorBoundary } from '@/widgets/error';

createRoot(document.getElementById('root')!).render(
	<StrictMode>
		<ErrorBoundary>
			<Provider store={store}>
				<RouterProvider router={appRouter} />
			</Provider>
		</ErrorBoundary>
	</StrictMode>
);

────────────────────────────────────────────────────────────────────────────────

FILE: src\app\appReducer.ts
────────────────────────────────────────────────────────────────────────────────

import { videosReducer } from '@/entities/video/index';
import { videosApi } from '@/entities/video/api/api';
import { combineReducers } from '@reduxjs/toolkit';

export const rootReducer = combineReducers({
	videos: videosReducer,
	[videosApi.reducerPath]: videosApi.reducer,
});

────────────────────────────────────────────────────────────────────────────────

FILE: src\app\appRouter.tsx
────────────────────────────────────────────────────────────────────────────────

import { createBrowserRouter } from 'react-router-dom';
import BaseLayout from './layout/BaseLayout';
import { MainPage } from '@/pages/home';
import { VideoPage } from '@/pages/video';
import { SearchPage } from '@/pages/search';
import { ErrorFallback } from '@/widgets/error';

export const appRouter = createBrowserRouter([
	{
		element: <BaseLayout />,
		errorElement: <ErrorFallback />,
		children: [
			{
				path: '/',
				element: <MainPage />,
			},
			{
				path: '/movie/:id',
				element: <VideoPage />,
			},
			{
				path: '/search',
				element: <SearchPage />,
			},
		],
	},
]);

────────────────────────────────────────────────────────────────────────────────

FILE: src\app\appStore.ts
────────────────────────────────────────────────────────────────────────────────

import { configureStore } from '@reduxjs/toolkit';
import { rootReducer } from './appReducer';
import { videosApi } from '@/entities/video/api/api';
import {
	useDispatch,
	useSelector,
	type TypedUseSelectorHook,
} from 'react-redux';

export const store = configureStore({
	reducer: rootReducer,
	middleware: getDefaultMiddleware =>
		getDefaultMiddleware().concat(videosApi.middleware),
});

export type RootState = ReturnType<typeof store.getState>;
export type AppDispatch = typeof store.dispatch;

export const useAppDispatch: () => AppDispatch = useDispatch;
export const useAppSelector: TypedUseSelectorHook<RootState> = useSelector;

────────────────────────────────────────────────────────────────────────────────

FILE: src\app\index.scss
────────────────────────────────────────────────────────────────────────────────

@use '@shared/styles/utils' as *;
@use '@shared/styles/utils/reset';
@use '@shared/styles/typography';
@use '@shared/styles/global';

────────────────────────────────────────────────────────────────────────────────

FILE: src\app\layout\BaseLayout.tsx
────────────────────────────────────────────────────────────────────────────────

import { Header, Footer } from '@/widgets';
import '@shared/styles/_global.scss';
import { Outlet, ScrollRestoration } from 'react-router-dom';

export default function BaseLayout() {
	return (
		<>
			<Header />

			<div className='container'>
				<ScrollRestoration />
				<Outlet />
			</div>

			<Footer />
		</>
	);
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\entities\video\api\api.ts
────────────────────────────────────────────────────────────────────────────────

import { createApi, fetchBaseQuery } from '@reduxjs/toolkit/query/react';
import type {
	Staff,
	Video,
	VideosApiResponse,
	SimilarFilmsResponse,
	SearchByKeywordResponse,
	SearchParams,
	VideoCategory,
	FramesMovieResponse,
	FilterParams,
} from '../model/types';

const BASE_URL = import.meta.env.VITE_VIDEOS_BASE_URL;
const STAFF_URL = import.meta.env.VITE_VIDEOS_STAFF_URL;
const SEARCH_URL = import.meta.env.VITE_VIDEOS_SEARCH_URL;
const API_KEY = import.meta.env.VITE_VIDEOS_API_KEY;

export const videosApi = createApi({
	reducerPath: 'videosApi',
	baseQuery: fetchBaseQuery({
		baseUrl: BASE_URL,
		prepareHeaders: headers => {
			headers.set('X-API-KEY', API_KEY);
			headers.set('Content-Type', 'application/json');
			return headers;
		},
	}),
	endpoints: builder => ({
		getMoviesByCategory: builder.query<VideosApiResponse, VideoCategory>({
			keepUnusedDataFor: 60,
			query: category => ({
				url: `films/collections`,
				params: {
					type: category,
					page: 1,
				},
			}),
		}),
		getSimilarMovies: builder.query<SimilarFilmsResponse, string>({
			keepUnusedDataFor: 60,
			query: id => ({
				url: `films/${id}/similars`,
				params: { filmId: id },
			}),
		}),
		getFramesOfMovie: builder.query<FramesMovieResponse, string>({
			keepUnusedDataFor: 60,
			query: id => ({
				url: `films/${id}/images`,
				params: { type: 'STILL', page: 1 },
			}),
		}),
		getMoviesByFilters: builder.query<VideosApiResponse, FilterParams>({
			keepUnusedDataFor: 60,
			query: filters => ({
				url: `films`,
				params: {
					...(filters.genre && { genre: filters.genre }),
					...(filters.country && { country: filters.country }),
					page: 1,
				},
			}),
		}),
		getMovieById: builder.query<Video, string>({
			keepUnusedDataFor: 60,
			query: id => `films/${id}`,
		}),
		getStaffById: builder.query<Staff[], string>({
			keepUnusedDataFor: 60,
			query: id => ({
				url: `${STAFF_URL}staff`,
				params: { filmId: id },
			}),
		}),
		getMovieBySearch: builder.query<SearchByKeywordResponse, string>({
			keepUnusedDataFor: 60,
			query: keyword => ({
				url: `${SEARCH_URL}/films/search-by-keyword`,
				params: { keyword },
			}),
		}),
	}),
});

export const {
	useGetMovieByIdQuery,
	useGetMovieBySearchQuery,
	useGetMoviesByCategoryQuery,
	useGetSimilarMoviesQuery,
	useGetStaffByIdQuery,
	useGetFramesOfMovieQuery,
	useGetMoviesByFiltersQuery,
} = videosApi;

────────────────────────────────────────────────────────────────────────────────

FILE: src\entities\video\index.ts
────────────────────────────────────────────────────────────────────────────────

export {
	videosApi,
	useGetMovieByIdQuery,
	useGetMovieBySearchQuery,
	useGetMoviesByCategoryQuery,
	useGetSimilarMoviesQuery,
	useGetStaffByIdQuery,
} from './api/api';

export { default as videosReducer } from './model/slice';
export { videosSlice } from './model/slice';
export type {
	Video,
	VideosApiResponse,
	SimilarFilm,
	SimilarFilmsResponse,
	Staff,
	SearchByKeywordResponse,
	SearchParams,
	VideoCategory,
} from './model/types';

export { default as VideoCard } from './ui/VideoCard/VideoCard';
export { default as VideoDetails } from './ui/VideoDetails/VideoDetails';
export { default as VideoFacts } from './ui/VideoFacts/VideoFacts';
export { default as VideoHero } from './ui/VideoHero/VideoHero';
export { default as VideoList } from './ui/VideoList/VideoList';
export { default as VideoMeta } from './ui/VideoMeta/VideoMeta';

export {
	formatGenres,
	formatCountries,
	getProducer,
	getActors,
} from './lib/formatFacts';

────────────────────────────────────────────────────────────────────────────────

FILE: src\entities\video\lib\formatFacts.tsx
────────────────────────────────────────────────────────────────────────────────

import type { Country, Genre, Staff } from '../model/types';

export const formatGenres = (genres: Genre[]) => {
	if (!genres?.length) return '-';
	return genres.map(({ genre }) => genre).join(', ');
};

export const formatCountries = (countries: Country[]) => {
	if (!countries?.length) return '-';
	return countries.map(({ country }) => country).join(', ');
};

export const getProducer = (staff?: Staff[]) => {
	if (!staff?.length) return '-';
	return staff.find(item => item.professionKey === 'PRODUCER')?.nameRu;
};

export const getActors = (staff?: Staff[], limit = 5) => {
	if (!staff?.length) return '-';
	return staff
		.filter(item => item.professionKey === 'ACTOR')
		.slice(0, limit)
		.map(actor => actor.nameRu)
		.join(', ');
};

────────────────────────────────────────────────────────────────────────────────

FILE: src\entities\video\model\slice.ts
────────────────────────────────────────────────────────────────────────────────

import { createSlice, type PayloadAction } from '@reduxjs/toolkit';
import type { Video } from './types';

interface State {
	videos: Video[];
}

const initialState: State = {
	videos: [],
};

export const videosSlice = createSlice({
	name: 'videos',
	initialState,
	reducers: {
		setVideos: (state, action: PayloadAction<Video[]>) => {
			state.videos = action.payload;
		},
	},
});

export default videosSlice.reducer;

────────────────────────────────────────────────────────────────────────────────

FILE: src\entities\video\model\types.ts
────────────────────────────────────────────────────────────────────────────────

export interface Video {
	filmId: number;
	kinopoiskId: number;
	imdbId: string;
	nameRu: string;
	nameEn: string;
	nameOriginal: string;
	posterUrl: string;
	posterUrlPreview: string;
	coverUrl: string;
	logoUrl: string;
	ratingKinopoisk: number;
	ratingImdb: number;
	description: string;
	shortDescription: string;
	countries: Country[];
	genres: Genre[];
	year: number;
	type: string;
}
export interface VideosApiResponse {
	total: number;
	totalPages: number;
	items: Video[];
}

export type VideoLike = Video | SimilarFilm;
export interface Genre {
	genre: string;
}
export interface Country {
	country: string;
}
export interface SimilarFilm {
	filmId: number;
	nameRu: string;
	nameEn: string;
	nameOriginal: string;
	posterUrl: string;
	posterUrlPreview: string;
	relationType: string;
}
export interface SimilarFilmsResponse {
	total: number;
	items: SimilarFilm[];
}
export interface Staff {
	staffId: number;
	nameRu: string;
	nameEn: string;
	description: string;
	posterUrl: string;
	professionText: string;
	professionKey: string;
}
export interface SearchByKeywordResponse {
	keyword: string;
	pagesCount: number;
	searchFilmsCountResult: number;
	films: Video[];
}

export type VideoCategory =
	| 'TOP_POPULAR_MOVIES'
	| 'TOP_250_MOVIES'
	| 'POPULAR_SERIES';

export interface FramesMovieResponse {
	total: number;
	totalPages: number;
	items: Frame[];
}

export interface Frame {
	imageUrl: string;
	previewUrl: string;
}

export interface FilterParams {
	genre: string;
	country: string;
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\entities\video\ui\FactItem\FactItem.tsx
────────────────────────────────────────────────────────────────────────────────

import Rating from '@/shared/ui/Rating/Rating';
import styles from './styles.module.scss';
import type { ReactNode } from 'react';
interface Props {
	label: string;
	value: ReactNode;
}

const FactItem = ({ label, value }: Props) => {
	return (
		<li className={styles.item}>
			<p>{label}</p>
			{label === 'Рейтинг' ? <Rating>{value}</Rating> : <p>{value}</p>}
		</li>
	);
};

export default FactItem;

────────────────────────────────────────────────────────────────────────────────

FILE: src\entities\video\ui\FactItem\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.item {
	display: flex;
	justify-content: space-between;
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\entities\video\ui\VideoCard\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.item {
	@include flex-column($gap: $spacing-sm);
	@include hover-scale(1.02);
	width: 100%;
	height: 100%;
	cursor: pointer;

	a {
		display: flex;
		flex-direction: column;
		gap: $spacing-sm;
		text-decoration: none;
		color: inherit;
	}
}

.poster {
	display: flex;
	flex-direction: column;
	overflow: hidden;
	gap: $spacing-sm;

	img {
		height: 500px;
	}
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\entities\video\ui\VideoCard\VideoCard.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';
import { Link } from 'react-router-dom';
import Image from '@/shared/ui/Image/Image';
import VideoMeta from '../VideoMeta/VideoMeta';
import type { Video, VideoLike } from '../../model/types';
import { memo } from 'react';

const isFullVideo = (video: VideoLike): video is Video =>
	'kinopoiskId' in video;

const getVideoId = (video: VideoLike) =>
	isFullVideo(video) ? video.kinopoiskId : video.filmId;

const getVideoTitle = (video: VideoLike) =>
	video.nameRu || video.nameEn || video.nameOriginal || 'Без названия';
interface VideoCardProps {
	video: VideoLike;
}

const VideoCard = memo(({ video }: VideoCardProps) => {
	return (
		<div className={styles.item}>
			<Link to={`/movie/${getVideoId(video)}`}>
				<div className={styles.poster}>
					<Image
						src={
							video.posterUrlPreview ??
							(isFullVideo(video) ? video.posterUrl : undefined)
						}
						alt={getVideoTitle(video)}
					/>
					<VideoMeta video={video} />
				</div>
			</Link>
		</div>
	);
});

export default VideoCard;

────────────────────────────────────────────────────────────────────────────────

FILE: src\entities\video\ui\VideoDetails\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.details {
	width: 100%;
	border-radius: $radius-sm;
	overflow: hidden;
	color: $color-primary;
	display: flex;
	flex-direction: column;
	gap: $spacing-2xl;
}

.title {
	font-size: $font-size-3xl;
}

.btns {
	display: flex;
	gap: $spacing-md;
	margin-bottom: 3rem;
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\entities\video\ui\VideoDetails\VideoDetails.tsx
────────────────────────────────────────────────────────────────────────────────

import { useNavigate } from 'react-router';
import styles from './styles.module.scss';
import Button from '@/shared/ui/Button/Button';
interface Props {
	id: string;
	title: string;
	description: string;
}

const VideoDetails = ({ id, title, description }: Props) => {
	const navigate = useNavigate();
	return (
		<div className={styles.details}>
			<h2 className={styles.title}>{title}</h2>
			<p className={styles.description}>{description}</p>
			<div className={styles.btns}>
				<Button
					variant='outline'
					onClick={() => navigate(`/movie/${id}`)}
					ariaLabel='Смотреть фильм'
				>
					Смотреть
				</Button>
				<Button
					variant='secondary'
					onClick={() => navigate(`/movie/${id}`)}
					ariaLabel='Добавить в избранное'
				>
					Избранное
				</Button>
			</div>
		</div>
	);
};

export default VideoDetails;

────────────────────────────────────────────────────────────────────────────────

FILE: src\entities\video\ui\VideoFacts\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.facts {
	color: $color-primary;
}

.title {
	margin-bottom: $spacing-lg;
}

.list {
	display: flex;
	flex-direction: column;
	gap: $spacing-lg;
	color: $color-text;
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\entities\video\ui\VideoFacts\VideoFacts.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';
import type { Staff, Video } from '../../model/types';
import FactItem from '../FactItem/FactItem';
import {
	formatCountries,
	formatGenres,
	getActors,
	getProducer,
} from '../../lib/formatFacts';
import { useMemo } from 'react';

interface Props {
	video: Video;
	staff: Staff[];
}

const VideoFacts = ({ video, staff }: Props) => {
	const facts = useMemo(
		() => [
			{ label: 'Жанр', value: formatGenres(video.genres) },
			{ label: 'Страна', value: formatCountries(video.countries) },
			{ label: 'Год', value: video.year },
			{ label: 'Режиссер', value: getProducer(staff) },
			{ label: 'В главных ролях', value: getActors(staff) },
			{ label: 'Рейтинг', value: video.ratingKinopoisk },
		],
		[video, staff]
	);

	return (
		<div className={styles.facts}>
			<h3 className={styles.title}>О фильме</h3>
			<ul className={styles.list}>
				{facts.map(({ label, value }) => (
					<FactItem key={label} label={label} value={value} />
				))}
			</ul>
		</div>
	);
};

export default VideoFacts;

────────────────────────────────────────────────────────────────────────────────

FILE: src\entities\video\ui\VideoHero\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.hero {
	color: $color-secondary;
	flex: 1;
}

.status {
	text-transform: uppercase;
	@include section-spacing(calc($spacing-3xl + $spacing-1xl));
}

.title {
	margin-bottom: $spacing-lg;
	font-size: $font-size-3xl;
}

.description {
	font-size: $font-size-lg;
	color: $color-secondary;
	@include section-spacing($spacing-3xl);
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\entities\video\ui\VideoHero\VideoHero.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';
import Button from '@/shared/ui/Button/Button';
import { useNavigate } from 'react-router';

interface Props {
	id: number;
	title: string;
	description: string;
}

const VideoHero = ({ id, title, description }: Props) => {
	const navigate = useNavigate();

	return (
		<article className={styles.hero}>
			<h3 className={styles.status}>уже в кино</h3>
			<h2 className={styles.title}>{title}</h2>
			<p className={styles.description}>{description}</p>
			<Button
				onClick={() => navigate(`/movie/${id}`)}
				ariaLabel='Смотреть фильм'
			>
				Смотреть
			</Button>
		</article>
	);
};

export default VideoHero;

────────────────────────────────────────────────────────────────────────────────

FILE: src\entities\video\ui\VideoList\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.list,
.listSkeleton {
	display: grid;
	grid-template-columns: repeat(auto-fit, 315px);
	gap: $spacing-lg;
	justify-content: space-evenly;
	align-content: start;

	@media (max-width: 1400px) {
		grid-template-columns: repeat(auto-fit, 315px);
	}

	@media (max-width: 1024px) {
		grid-template-columns: repeat(auto-fit, minmax(280px, 315px));
	}

	@media (max-width: 640px) {
		grid-template-columns: minmax(0, 1fr);
		justify-content: stretch;
	}
}

.itemSkeleton {
	@include skeleton-pulse;
	width: 315px;
	height: 315px;
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\entities\video\ui\VideoList\VideoList.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';
import VideoCard from '@/entities/video/ui/VideoCard/VideoCard';
import type { VideoLike } from '../../model/types';
import { memo } from 'react';

interface VideoListProps {
	items: VideoLike[];
	limit: number;
}

const VideoList = memo(({ items, limit }: VideoListProps) => {
	const list = items.slice(0, limit);

	return (
		<ul className={styles.list}>
			{list.map(item => {
				const key =
					'kinopoiskId' in item && item.kinopoiskId
						? item.kinopoiskId
						: item.filmId;

				return <VideoCard key={key} video={item} />;
			})}
		</ul>
	);
});

export default VideoList;

────────────────────────────────────────────────────────────────────────────────

FILE: src\entities\video\ui\VideoList\VideoListSkeleton.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';

interface Props {
	count: number;
}

const VideoListSkeleton = ({ count }: Props) => {
	return (
		<ul className={styles.listSkeleton}>
			{Array.from({ length: count }, (_, i) => (
				<li key={i} className={styles.itemSkeleton} />
			))}
		</ul>
	);
};

export default VideoListSkeleton;

────────────────────────────────────────────────────────────────────────────────

FILE: src\entities\video\ui\VideoMeta\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.meta {
	display: flex;
	justify-content: space-between;
}

.info {
	display: flex;
	flex-direction: column;
	gap: $spacing-xs;
}

.title {
	color: $color-primary;
}

.tag {
	align-self: flex-end;
	padding: $spacing-2sm $spacing-sm;
	border-radius: $radius-sm;
	background-color: rgba($color-text, 0.15);
	color: $color-primary;
	font-size: $font-size-sm;
	text-transform: lowercase;
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\entities\video\ui\VideoMeta\VideoMeta.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';
import Year from '@/shared/ui/Year/Year';
import type { Video, VideoLike } from '../../model/types';
import Rating from '@/shared/ui/Rating/Rating';

const isFullVideo = (video: VideoLike): video is Video =>
	'kinopoiskId' in video;
interface Props {
	video: VideoLike;
}

const VideoMeta = ({ video }: Props) => {
	const title =
		video.nameRu || video.nameEn || video.nameOriginal || 'Без названия';

	const isMovie = isFullVideo(video);
	const year = isMovie ? video.year : undefined;
	const rating = isMovie ? video.ratingKinopoisk : undefined;

	return (
		<div className={styles.meta}>
			<div className={styles.info}>
				<h4 className={styles.title}>{title}</h4>
				<Year>{year}</Year>
			</div>
			<Rating>{rating}</Rating>
		</div>
	);
};

export default VideoMeta;

────────────────────────────────────────────────────────────────────────────────

FILE: src\features\breadcrumbs\ui\Breadcrumbs\Breadcrumbs.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';
import { Link } from 'react-router-dom';
import Separator from '@/shared/ui/Separator/Separator';
import type { ReactNode } from 'react';
import type { DirectionType } from '@/shared/types/types';
interface BreadcrumbsProps {
	children: ReactNode;
	to: string;
	direction: DirectionType;
}
const Breadcrumbs = ({
	children,
	to,
	direction = 'next',
}: BreadcrumbsProps) => {
	return (
		<div
			className={`${styles.breadcrumbs} ${
				direction === 'next' ? styles.next : styles.back
			}`}
		>
			<Link to={to} className={styles.link}>
				{direction === 'back' && <Separator direction='back' />}
				{children}
				{direction === 'next' && <Separator direction='next' />}
			</Link>
		</div>
	);
};

export default Breadcrumbs;

────────────────────────────────────────────────────────────────────────────────

FILE: src\features\breadcrumbs\ui\Breadcrumbs\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.breadcrumbs {
	display: flex;

	&.next {
		justify-content: flex-end;
	}

	&.back {
		justify-content: flex-start;
	}
}

.link {
	@include flex-align($gap: $spacing-md);
	@include transition(color);
	margin-bottom: $spacing-lg;

	&:hover {
		color: $color-accent;
	}
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\features\filterVideos\ui\FilterPanel\FilterPanel.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';
import { genres, countries } from '@/shared/constants/info';
import Button from '@/shared/ui/Button/Button';
import Select from '@/shared/ui/Select/Select';
import { useState } from 'react';
interface FilterPanelProps {
	onFiltersChange: (filters: { genre: string; country: string }) => void;
}

const FilterPanel = ({ onFiltersChange }: FilterPanelProps) => {
	const [genreValue, setGenreValue] = useState('');
	const [countryValue, setCountryValue] = useState('');

	const handleFilter = () => {
		const filters = {
			genre: genreValue,
			country: countryValue,
		};
		onFiltersChange(filters);
	};

	const handleReset = () => {
		setGenreValue('');
		setCountryValue('');
		onFiltersChange({ genre: '', country: '' });
	};

	return (
		<div className={styles.filters}>
			<Select
				value={genreValue}
				onChange={setGenreValue}
				options={[{ value: '', label: 'Все жанры' }, ...genres]}
			/>
			<Select
				value={countryValue}
				onChange={setCountryValue}
				options={[{ value: '', label: 'Все страны' }, ...countries]}
			/>
			<Button onClick={handleFilter} variant='outline'>
				Поиск
			</Button>
			{(genreValue || countryValue) && (
				<Button onClick={handleReset} variant='secondary'>
					Сбросить
				</Button>
			)}
		</div>
	);
};

export default FilterPanel;

────────────────────────────────────────────────────────────────────────────────

FILE: src\features\filterVideos\ui\FilterPanel\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.filters {
	@include flex-align($gap: $spacing-lg);
	margin-top: $spacing-2xl;
	@include section-spacing($spacing-3xl);
	flex-wrap: wrap;
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\features\index.ts
────────────────────────────────────────────────────────────────────────────────

import Breadcrumbs from './breadcrumbs/ui/Breadcrumbs/Breadcrumbs';
import FilterPanel from './filterVideos/ui/FilterPanel/FilterPanel';
import SearchVideo from './searchVideo/ui/SearchVideo/SearchVideo';

export { Breadcrumbs, FilterPanel, SearchVideo };

────────────────────────────────────────────────────────────────────────────────

FILE: src\features\searchVideo\ui\SearchVideo\SearchVideo.tsx
────────────────────────────────────────────────────────────────────────────────

import React, { useState, useEffect } from 'react';
import styles from './styles.module.scss';
import searchIconUrl from '@shared/assets/icons/search.svg';
import { useNavigate } from 'react-router-dom';
import { useDebounce } from '@/shared/hooks/useDebounce';

const SearchVideo = () => {
	const [query, setQuery] = useState('');
	const debouncedQuery = useDebounce(query, 500);
	const navigate = useNavigate();

	useEffect(() => {
		if (debouncedQuery.trim()) {
			navigate(`/search?query=${debouncedQuery.trim()}`);
		}
	}, [debouncedQuery, navigate]);

	const handleChange = (e: React.ChangeEvent<HTMLInputElement>) => {
		setQuery(e.target.value);
	};

	const handleSubmit = () => {
		const value = query.trim();
		if (value) {
			navigate(`/search?query=${value}`);
		}
	};

	return (
		<div className={styles.searchWrapper}>
			<input
				type='text'
				placeholder='Поиск фильмов...'
				className={styles.searchInput}
				value={query}
				onChange={handleChange}
				onKeyDown={e => e.key === 'Enter' && handleSubmit()}
			/>
			<img
				className={styles.searchIcon}
				src={searchIconUrl}
				alt='поиск фильмов'
				onClick={handleSubmit}
			/>
		</div>
	);
};

export default SearchVideo;

────────────────────────────────────────────────────────────────────────────────

FILE: src\features\searchVideo\ui\SearchVideo\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.searchWrapper {
	position: relative;
	width: 100%;
	max-width: 400px;
}

.searchInput {
	width: 100%;
	@include padding-xy($spacing-lg, $spacing-2sm);
	padding-right: $spacing-2xl;
	@include focus-border($color-accent);
	@include transition(border-color);
	border: 2px solid rgba(0, 0, 0, 0.1);
	border-radius: $radius-lg;
	font-size: $font-size-md;
}

.searchIcon {
	@include absolute-vertical-center;
	right: $spacing-lg;
	cursor: pointer;
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\pages\home\index.ts
────────────────────────────────────────────────────────────────────────────────

import MainPage from './ui/HomePage';

export { MainPage };

────────────────────────────────────────────────────────────────────────────────

FILE: src\pages\home\ui\HomePage.tsx
────────────────────────────────────────────────────────────────────────────────

import { HeroBanner, ContentCatalog, VideoFilters } from '@/widgets';

const HomePage = () => {
	return (
		<main>
			<HeroBanner />
			<ContentCatalog />
			<VideoFilters />
		</main>
	);
};

export default HomePage;

────────────────────────────────────────────────────────────────────────────────

FILE: src\pages\search\index.ts
────────────────────────────────────────────────────────────────────────────────

import SearchPage from './ui/SearchPage';

export { SearchPage };

────────────────────────────────────────────────────────────────────────────────

FILE: src\pages\search\ui\SearchPage.tsx
────────────────────────────────────────────────────────────────────────────────

import Title from '@/shared/ui/Title/Title';
import SearchResults from '@/widgets/searchResults/SearchResults';

const SearchPage = () => {
	return (
		<main>
			<Title>Результаты поиска</Title>
			<SearchResults />
		</main>
	);
};

export default SearchPage;

────────────────────────────────────────────────────────────────────────────────

FILE: src\pages\video\index.ts
────────────────────────────────────────────────────────────────────────────────

import VideoPage from './ui/VideoPage';

export { VideoPage };

────────────────────────────────────────────────────────────────────────────────

FILE: src\pages\video\ui\VideoPage.tsx
────────────────────────────────────────────────────────────────────────────────

import VideoDescription from '@/widgets/videoDescription/VideoDescription';
import { Breadcrumbs } from '@/features';
import VideoFrames from '@/widgets/videoFrames/ui/VideoFrames';
import VideoSimilar from '@/widgets/videoSimilar/VideoSimilar';
import { useParams } from 'react-router';

const VideoPage = () => {
	const { id } = useParams<{ id: string }>();

	return (
		<main>
			<Breadcrumbs to={'/'} direction={'back'}>
				Главная
			</Breadcrumbs>
			<VideoDescription filmId={id!} />
			<VideoFrames filmId={id!} />
			<VideoSimilar filmId={id!} />
		</main>
	);
};

export default VideoPage;

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\assets\icons\project_context.txt
────────────────────────────────────────────────────────────────────────────────

# PROJECT CONTEXT
# Generated: 2025-11-16T15:18:01.649Z

================================================================================

## PROJECT STRUCTURE

  index.html
  package.json
  src/app/layout/BaseLayout.tsx
  src/components/breadcrumbItem/BreadcrumbItem.tsx
  src/components/breadcrumbItem/styles.module.scss
  src/components/breadcrumbs/Breadcrumbs.tsx
  src/components/breadcrumbs/styles.module.scss
  src/components/Separator/Separator.tsx
  src/components/footer/Footer.tsx
  src/components/footer/styles.module.scss
  src/components/header/Header.tsx
  src/components/header/styles.module.scss
  src/components/image/Image.tsx
  src/components/image/styles.module.scss
  src/components/logo/Logo.tsx
  src/components/logo/styles.module.scss
  src/components/HeroBanner/HeroBanner.tsx
  src/components/HeroBanner/styles.module.scss
  src/components/VideoCard/VideoCard.tsx
  src/components/VideoCard/styles.module.scss
  src/components/VideoDescription/VideoDescription.tsx
  src/components/VideoDescription/styles.module.scss
  src/components/movieDetails/MovieDetails.tsx
  src/components/movieDetails/styles.module.scss
  src/components/FilterPanel/FilterPanel.tsx
  src/components/FilterPanel/styles.module.scss
  src/components/FilterPanel/FilterPanel.tsx
  src/components/FilterPanel/styles.module.scss
  src/components/VideoList/VideoList.tsx
  src/components/VideoList/styles.module.scss
  src/components/ContentCatalog/ContentCatalog.tsx
  src/components/navigation/Navigation.tsx
  src/components/navigation/styles.module.scss
  src/components/HeroDescription/HeroDescription.tsx
  src/components/HeroDescription/styles.module.scss
  src/components/search/Search.tsx
  src/components/search/styles.module.scss
  src/components/select/Select.tsx
  src/components/select/styles.module.scss
  src/components/sortControls/SortControls.tsx
  src/components/sortControls/styles.module.scss
  src/components/tab/Tab.tsx
  src/components/tab/styles.module.scss
  src/components/tabs/Tabs.tsx
  src/components/tabs/styles.module.scss
  src/main.tsx
  src/pages/mainPage/MainPage.tsx
  src/pages/moviePage/MoviePage.tsx
  src/pages/searchPage/SearchPage.tsx
  src/styles/_global.scss
  src/styles/_typography.scss
  src/styles/index.scss
  src/styles/utils/_index.scss
  src/styles/utils/_mixins.scss
  src/styles/utils/_reset.scss
  src/styles/utils/_variables.scss
  src/ui/button/Button.tsx
  src/ui/button/styles.module.scss
  tsconfig.json
  tsconfig.node.json
  vite.config.ts

================================================================================

## FILES CONTENT

────────────────────────────────────────────────────────────────────────────────
FILE: index.html
────────────────────────────────────────────────────────────────────────────────

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>yeamovie</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>


────────────────────────────────────────────────────────────────────────────────
FILE: package.json
────────────────────────────────────────────────────────────────────────────────

{
	"name": "yeamovie",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "tsc -b && vite build",
		"lint": "eslint .",
		"preview": "vite preview",
		"collect": "node collect-project.mjs"
	},
	"dependencies": {
		"react": "^19.2.0",
		"react-dom": "^19.2.0",
		"react-router-dom": "^7.9.6"
	},
	"devDependencies": {
		"@eslint/js": "^9.39.1",
		"@types/node": "^24.10.1",
		"@types/react": "^19.2.2",
		"@types/react-dom": "^19.2.2",
		"@vitejs/plugin-react": "^5.1.0",
		"eslint": "^9.39.1",
		"eslint-plugin-react-hooks": "^7.0.1",
		"eslint-plugin-react-refresh": "^0.4.24",
		"globals": "^16.5.0",
		"sass-embedded": "^1.93.3",
		"scss-reset": "^1.4.6",
		"typescript": "~5.9.3",
		"typescript-eslint": "^8.46.3",
		"vite": "^7.2.2"
	}
}


────────────────────────────────────────────────────────────────────────────────
FILE: src/app/layout/BaseLayout.tsx
────────────────────────────────────────────────────────────────────────────────

import Footer from '@/components/footer/Footer';
import Header from '@/components/header/Header';
import '@styles/_global.scss';
import { Outlet } from 'react-router-dom';

const BaseLayout = () => {
	return (
		<>
			<Header />

			<div className='container'>
				<Outlet />
			</div>

			<Footer />
		</>
	);
};

export default BaseLayout;


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/breadcrumbItem/BreadcrumbItem.tsx
────────────────────────────────────────────────────────────────────────────────

import Separator from '../Separator/Separator';
import styles from './styles.module.scss';
import { Link } from 'react-router-dom';

const BreadcrumbItem = ({ children, to, direction = 'next' }) => {
	return (
		<Link to={to}>
			<div
				className={`${styles.breadcrumbsItem} ${
					direction === 'next' ? styles.next : styles.prev
				}`}
			>
				{direction === 'back' && <Separator direction='left' />}
				{children}
				{direction === 'next' && <Separator direction='right' />}
			</div>
		</Link>
	);
};

export default BreadcrumbItem;


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/breadcrumbItem/styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.breadcrumbsItem {
	@include flex-align($gap: $spacing-md);
	@include transition(color);
	margin-bottom: $spacing-lg;

	&:hover {
		color: $color-accent;
	}

	&.next {
		margin-left: auto;
	}

	&.prev {
		margin-right: auto;
	}
}


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/breadcrumbs/Breadcrumbs.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';

const Breadcrumbs = ({ children }) => {
	return <nav className={styles.breadcrumbs}>{children}</nav>;
};

export default Breadcrumbs;


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/breadcrumbs/styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.breadcrumbs {
	display: flex;
}


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/Separator/Separator.tsx
────────────────────────────────────────────────────────────────────────────────

import arrowLeft from '@assets/icons/arrowLeft.svg';
import arrowRight from '@assets/icons/arrowRight.svg';
import arrowDown from '@assets/icons/arrowDown.svg';
import arrowUp from '@assets/icons/arrowUp.svg';
import arrowSelect from '@assets/icons/arrowSelect.svg';

const Separator = ({ direction = 'left' }) => {
	const icons = {
		left: arrowLeft,
		right: arrowRight,
		up: arrowUp,
		down: arrowDown,
		select: arrowSelect,
	};
	return <img src={icons[direction]} alt={direction} />;
};

export default Separator;


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/footer/Footer.tsx
────────────────────────────────────────────────────────────────────────────────

import Logo from '../logo/Logo';
import Navigation from '../navigation/Navigation';
import styles from './styles.module.scss';

const Footer = () => {
	return (
		<footer className={styles.footer}>
			<Logo />
			<Navigation />
		</footer>
	);
};

export default Footer;


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/footer/styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.footer {
	@include flex-between;
	padding: $spacing-2xl;
	background-color: $color-primary;
}


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/header/Header.tsx
────────────────────────────────────────────────────────────────────────────────

import Logo from '../logo/Logo';
import Search from '../search/Search';
import styles from './styles.module.scss';

const Header = () => {
	return (
		<header className={styles.header}>
			<Logo />
			<Search />
		</header>
	);
};

export default Header;


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/header/styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.header {
	@include flex-align($gap: 124px);
	@include padding-xy(calc($spacing-2xl * 2), $spacing-md);
	font-weight: 400;
	background-color: $color-primary;
	color: $color-accent;
}

.logo {
	font-family: 'Bayon';
	letter-spacing: 5px;
}


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/image/Image.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';

const Image = ({ src, alt }) => {
	return (
		<div className={styles.image}>
			<img src={src} alt={alt} />
		</div>
	);
};

export default Image;


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/image/styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.image {
	@include rounded($radius-sm);

	img {
		@include responsive-image;
	}
}


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/logo/Logo.tsx
────────────────────────────────────────────────────────────────────────────────

import { Link } from 'react-router-dom';
import styles from './styles.module.scss';

const Logo = () => {
	return (
		<Link to={'/'}>
			<h2 className={styles.logo}>kinomonster</h2>
		</Link>
	);
};

export default Logo;


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/logo/styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.logo {
	font-family: 'Bayon';
	letter-spacing: 5px;
	color: $color-accent;
	text-transform: uppercase;
}


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/HeroBanner/HeroBanner.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';
import banner from '@assets/images/banner.png';
import HeroDescription from '@components/HeroDescription/HeroDescription';
import Image from '@components/image/Image';

const HeroBanner = () => {
	return (
		<section className={styles.banner}>
			<HeroDescription movieName={'Чужой: Ромул (2024)'} />
			<Image src={banner} alt={'banner'} />
		</section>
	);
};

export default HeroBanner;


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/HeroBanner/styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.banner {
	@include flex-align($gap: calc($spacing-xl * 1.25));
	@include section-spacing($spacing-3xl);
	@include rounded($radius-sm);
	padding: $spacing-xl;
	background-color: $color-primary;
}

.headerText {
	text-transform: uppercase;
	margin-bottom: calc($spacing-3xl + 40px);
}


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/VideoCard/VideoCard.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';
import previewCard from '@assets/images/previewCard.png';
import Image from '../image/Image';
import VideoDescription from '../VideoDescription/VideoDescription';
import { Link } from 'react-router-dom';

const VideoCard = ({ title, year, rating }) => {
	return (
		<li className={styles.item}>
			<Link to='movie'>
				<Image src={previewCard} alt={title} />
				<VideoDescription title={title} year={year} rating={rating} />
			</Link>
		</li>
	);
};

export default VideoCard;


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/VideoCard/styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.item {
	@include flex-column($gap: $spacing-sm);
	@include hover-scale(1.02);
	cursor: pointer;
}


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/VideoDescription/VideoDescription.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';

const VideoDescription = ({ title, year, rating }) => {
	return (
		<div className={styles.description}>
			<div className={styles.info}>
				<h4 className={styles.title} title={title}>
					{title}
				</h4>
				<span className={styles.year} aria-label='Год выпуска'>
					{year}
				</span>
			</div>
			<span className={styles.rating} aria-label='Рейтинг'>
				⭐ {rating.toFixed(1)}
			</span>
		</div>
	);
};

export default VideoDescription;


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/VideoDescription/styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.description {
	@include flex-align($gap: $spacing-2sm);
}

.title {
	@include text-ellipsis;
	@include transition(color);
	font-size: $font-size-lg;
	font-weight: 600;
	color: $color-primary;

	&:hover {
		color: $color-accent;
	}
}

.info {
	@include flex-column($gap: $spacing-xs);
	flex: 1;
	font-size: $font-size-sm;
}

.year {
	flex: 1;
}

.rating {
	align-self: flex-end;
}


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/movieDetails/MovieDetails.tsx
────────────────────────────────────────────────────────────────────────────────

import BreadcrumbItem from '../breadcrumbItem/BreadcrumbItem';
import Breadcrumbs from '../breadcrumbs/Breadcrumbs';
import HeroBanner from '../HeroBanner/HeroBanner';
import HeroDescription from '../HeroDescription/HeroDescription';
import styles from './styles.module.scss';

const MovieDetails = () => {
	return (
		<div className={styles.details}>
			<Breadcrumbs>
				<BreadcrumbItem to='/' direction='back'>
					Главная
				</BreadcrumbItem>
			</Breadcrumbs>
			<HeroBanner />
		</div>
	);
};

export default MovieDetails;


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/movieDetails/styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.details {
	margin-bottom: $spacing-3xl;
}


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/FilterPanel/FilterPanel.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';
import Select from '../select/Select';
import SortControls from '../sortControls/SortControls';

const genres = [
	{ value: 'horror', label: 'Ужасы' },
	{ value: 'action', label: 'Боевик' },
	{ value: 'comedy', label: 'Комедия' },
	{ value: 'drama', label: 'Драма' },
	{ value: 'thriller', label: 'Триллер' },
	{ value: 'fantasy', label: 'Фантастика' },
	{ value: 'detective', label: 'Детектив' },
	{ value: 'romance', label: 'Романтика' },
	{ value: 'adventure', label: 'Приключения' },
	{ value: 'animation', label: 'Мультфильм' },
];

const countries = [
	{ value: 'russia', label: 'Россия' },
	{ value: 'usa', label: 'США' },
	{ value: 'uk', label: 'Великобритания' },
	{ value: 'france', label: 'Франция' },
	{ value: 'germany', label: 'Германия' },
	{ value: 'italy', label: 'Италия' },
	{ value: 'spain', label: 'Испания' },
	{ value: 'japan', label: 'Япония' },
	{ value: 'korea', label: 'Южная Корея' },
	{ value: 'china', label: 'Китай' },
];

const years = [
	{ value: '2024', label: '2024' },
	{ value: '2023', label: '2023' },
	{ value: '2022', label: '2022' },
	{ value: '2021', label: '2021' },
	{ value: '2020', label: '2020' },
	{ value: '2019', label: '2019' },
	{ value: '2018', label: '2018' },
	{ value: '2017', label: '2017' },
	{ value: '2016', label: '2016' },
	{ value: '2015', label: '2015' },
];

const ages = [
	{ value: '0', label: '0+' },
	{ value: '6', label: '6+' },
	{ value: '12', label: '12+' },
	{ value: '16', label: '16+' },
	{ value: '18', label: '18+' },
];

const productions = [
	{ value: 'russia', label: 'Россия' },
	{ value: 'usa', label: 'США' },
	{ value: 'europe', label: 'Европа' },
	{ value: 'asia', label: 'Азия' },
];

const FilterPanel = () => {
	return (
		<div className={styles.filters}>
			<Select options={genres} />
			<Select options={countries} />
			<Select options={years} />
			<Select options={ages} />
			<Select options={productions} />
			<SortControls />
		</div>
	);
};

export default FilterPanel;


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/FilterPanel/styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.filters {
	@include flex-align($gap: $spacing-lg);
	margin-top: $spacing-2xl;
	@include section-spacing($spacing-3xl);
}


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/FilterPanel/FilterPanel.tsx
────────────────────────────────────────────────────────────────────────────────

import FilterPanel from '../FilterPanel/FilterPanel';
import VideoList from '../VideoList/VideoList';
import Tab from '../tab/Tab';

const FilterPanel = () => {
	return (
		<section>
			<Tab>Фильмы по категориям</Tab>
			<FilterPanel />
			<VideoList />
		</section>
	);
};

export default FilterPanel;


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/FilterPanel/styles.module.scss
────────────────────────────────────────────────────────────────────────────────



────────────────────────────────────────────────────────────────────────────────
FILE: src/components/VideoList/VideoList.tsx
────────────────────────────────────────────────────────────────────────────────

import VideoCard from '../VideoCard/VideoCard';
import styles from './styles.module.scss';

const movies = [
	{
		title: 'Интерстеллар',
		year: 2014,
		rating: 8.6,
	},
	{
		title: 'Начало',
		year: 2010,
		rating: 8.8,
	},
	{
		title: 'Темный рыцарь',
		year: 2008,
		rating: 9.0,
	},
	{
		title: 'Бойцовский клуб',
		year: 1999,
		rating: 8.8,
	},
	{
		title: 'Матрица',
		year: 1999,
		rating: 8.7,
	},
	{
		title: 'Побег из Шоушенка',
		year: 1994,
		rating: 9.3,
	},
	{
		title: 'Форрест Гамп',
		year: 1994,
		rating: 8.8,
	},
	{
		title: 'Криминальное чтиво',
		year: 1994,
		rating: 8.9,
	},
];

const VideoList = () => {
	return (
		<ul className={styles.list}>
			{movies &&
				movies?.map(movie => (
					<VideoCard
						key={movie.title}
						title={movie.title}
						year={movie.year}
						rating={movie.rating}
					/>
				))}
		</ul>
	);
};

export default VideoList;


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/VideoList/styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.list {
	@include grid-responsive(4, 2, 1, $spacing-lg);
	@include section-spacing(calc(6.25 * $spacing-md));
}


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/ContentCatalog/ContentCatalog.tsx
────────────────────────────────────────────────────────────────────────────────

import Breadcrumbs from '@components/breadcrumbs/Breadcrumbs';
import BreadcrumbItem from '@components/breadcrumbItem/BreadcrumbItem';
import VideoList from '@components/VideoList/VideoList';
import Tab from '@components/tab/Tab';
import Tabs from '@components/tabs/Tabs';

const ContentCatalog = () => {
	return (
		<section>
			{/* onTabChange={handleTabChange} */}
			<Tabs>
				<Tab>Популярные фильмы</Tab>
				<Tab>Популярные сериалы</Tab>
				<Tab>Подборка фильмов</Tab>
			</Tabs>
			<Breadcrumbs>
				<BreadcrumbItem children={'Смотреть все'} to={'movie'} />
			</Breadcrumbs>
			<VideoList />
		</section>
	);
};

export default ContentCatalog;


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/navigation/Navigation.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';
import { Link } from 'react-router-dom';

const Navigation = () => {
	return (
		<nav className={styles.navigation}>
			<Link to={'/'}>Главная</Link>
			<Link to={'/popular-movies'}>Популярные фильмы</Link>
			<Link to={'/popular-series'}>Популярные сериалы</Link>
		</nav>
	);
};

export default Navigation;


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/navigation/styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.navigation {
	@include flex-align($gap: $spacing-1xl);
	color: $color-text-lighter;

	a {
		@include transition(color);

		&:hover {
			color: $color-accent;
		}
	}
}


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/HeroDescription/HeroDescription.tsx
────────────────────────────────────────────────────────────────────────────────

import Button from '@/ui/button/Button';
import styles from './styles.module.scss';

const HeroDescription = ({ movieName }) => {
	return (
		<article className={styles.poster}>
			<h3 className={styles.headerText}>уже в кино</h3>
			<h1 className={styles.movieName}>{movieName}</h1>
			<p className={styles.description}>
				Исследуя заброшенную космическую станцию, группа колонизаторов
				сталкивается с самой ужасающей формой жизни во Вселенной.
			</p>
			<Button onClick={() => 'click'} ariaLabel='Смотреть фильм'>
				Смотреть
			</Button>
		</article>
	);
};

export default HeroDescription;


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/HeroDescription/styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.poster {
	color: $color-secondary;
	flex: 1;
}

.headerText {
	text-transform: uppercase;
	margin-bottom: calc($spacing-3xl + 40px);
}

.movieName {
	margin-bottom: $spacing-lg;
}

.description {
	font-size: $font-size-lg;
	@include section-spacing($spacing-3xl);
}


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/search/Search.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';
import searchIconUrl from '@assets/icons/search.svg';

const Search = () => {
	return (
		<div className={styles.searchWrapper}>
			<input
				type='text'
				placeholder='Поиск фильмов...'
				className={styles.searchInput}
			/>
			<img
				className={styles.searchIcon}
				src={searchIconUrl}
				alt='поиск фильмов'
			/>
		</div>
	);
};

export default Search;


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/search/styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.searchWrapper {
	position: relative;
	width: 100%;
	max-width: 400px;
}

.searchInput {
	width: 100%;
	@include padding-xy($spacing-lg, $spacing-2sm);
	padding-right: $spacing-2xl;
	@include focus-border($color-accent);
	@include transition(border-color);
	border: 2px solid rgba(0, 0, 0, 0.1);
	border-radius: $radius-lg;
	font-size: $font-size-md;
}

.searchIcon {
	@include absolute-vertical-center;
	right: $spacing-lg;
	cursor: pointer;
}


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/select/Select.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';

const Select = ({ options }) => {
	return (
		<select className={styles.select}>
			{options?.map(option => (
				<option
					className={styles.option}
					key={option.value}
					value={option.value}
				>
					{option.label}
				</option>
			))}
		</select>
	);
};

export default Select;


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/select/styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.select {
	appearance: none;
	@include rounded($radius-lg);
	@include padding-xy($spacing-md, $spacing-2sm);
	@include focus-border(rgba($color-text, 0.5));
	@include transition(border-color);
	padding-right: $spacing-xl;
	color: $color-text;
	border-color: $color-text;
	background-color: transparent;
	background-image: url('@assets/icons/arrowSelect.svg');
	background-repeat: no-repeat;
	background-position: right $spacing-sm center;
	background-size: 16px;

	&:hover {
		cursor: pointer;
	}
}


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/sortControls/SortControls.tsx
────────────────────────────────────────────────────────────────────────────────

import Separator from '../Separator/Separator';
import Button from '@/ui/button/Button';

const SortControls = () => {
	return (
		<Button variant='icon' ariaLabel='Сортировка'>
			<Separator direction='down' />
			<Separator direction='up' />
		</Button>
	);
};

export default SortControls;


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/sortControls/styles.module.scss
────────────────────────────────────────────────────────────────────────────────



────────────────────────────────────────────────────────────────────────────────
FILE: src/components/tab/Tab.tsx
────────────────────────────────────────────────────────────────────────────────

import Button from '@/ui/button/Button';
import styles from './styles.module.scss';

const Tab = ({ children, isActive = true, onClick }) => {
	return (
		<Button active={isActive} variant='secondary' onClick={onClick}>
			{children}
		</Button>
	);
};

export default Tab;


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/tab/styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.tab {
	background-color: $color-secondary;
	border-radius: $radius-lg;
	color: $color-primary;
	padding: $spacing-2sm $spacing-xl;
	font-size: $font-size-md;

	&.active {
		background-color: $color-primary;
		color: $color-secondary;
	}
}


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/tabs/Tabs.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';

const Tabs = ({ children, activeIndex = 0, onChange }) => {
	return <section className={styles.tabs}>{children}</section>;
};

export default Tabs;


────────────────────────────────────────────────────────────────────────────────
FILE: src/components/tabs/styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.tabs {
	@include flex-align(50);
	margin-bottom: $spacing-3xl;
}


────────────────────────────────────────────────────────────────────────────────
FILE: src/main.tsx
────────────────────────────────────────────────────────────────────────────────

import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import './styles/index.scss';
import { BrowserRouter, Routes, Route } from 'react-router-dom';
import MoviePage from './pages/moviePage/MoviePage.tsx';
import SearchPage from './pages/searchPage/SearchPage.tsx';
import BaseLayout from './app/layout/BaseLayout.tsx';
import MainPage from './pages/mainPage/MainPage.tsx';

createRoot(document.getElementById('root')!).render(
	<StrictMode>
		<BrowserRouter>
			<Routes>
				<Route path='/' element={<BaseLayout />}>
					<Route index element={<MainPage />} />
					<Route path='movie' element={<MoviePage />} />
					<Route path='search' element={<SearchPage />} />
				</Route>
			</Routes>
		</BrowserRouter>
	</StrictMode>
);


────────────────────────────────────────────────────────────────────────────────
FILE: src/pages/mainPage/MainPage.tsx
────────────────────────────────────────────────────────────────────────────────

import HeroBanner from '@/components/HeroBanner/HeroBanner';
import FilterPanel from '@/components/FilterPanel/FilterPanel';
import ContentCatalog from '@/components/ContentCatalog/ContentCatalog';

const MainPage = () => {
	return (
		<main>
			<HeroBanner />
			<ContentCatalog />
			<FilterPanel />
		</main>
	);
};

export default MainPage;


────────────────────────────────────────────────────────────────────────────────
FILE: src/pages/moviePage/MoviePage.tsx
────────────────────────────────────────────────────────────────────────────────

import MovieDetails from '@/components/movieDetails/MovieDetails';

const MoviePage = () => {
	return (
		<div>
			<MovieDetails />
		</div>
	);
};

export default MoviePage;


────────────────────────────────────────────────────────────────────────────────
FILE: src/pages/searchPage/SearchPage.tsx
────────────────────────────────────────────────────────────────────────────────

const SearchPage = () => {
	return <div>SearchPage</div>;
};

export default SearchPage;


────────────────────────────────────────────────────────────────────────────────
FILE: src/styles/_global.scss
────────────────────────────────────────────────────────────────────────────────

@use 'utils' as *;

.container {
	margin: 0 auto;
	padding: $spacing-3xl;
}


────────────────────────────────────────────────────────────────────────────────
FILE: src/styles/_typography.scss
────────────────────────────────────────────────────────────────────────────────

@use 'utils/variables' as *;

@font-face {
	font-family: 'MyFont';
	src: url('@assets/fonts/MyFont.ttf') format('truetype');
	font-weight: 400;
	font-style: normal;
	font-display: swap;
}

@font-face {
	font-family: 'Bayon';
	src: url('@assets/fonts/Bayon.ttf') format('truetype');
	font-weight: 300;
	font-style: normal;
	font-display: swap;
}

h1,
h2,
h3,
h4,
h5,
h6 {
	margin: 0;
	font-weight: 600;
	line-height: 1.2;
}

h1 {
	font-size: $font-size-3xl;
	letter-spacing: 5px;
}

h2 {
	font-size: $font-size-2xl;
}

h3 {
	font-size: $font-size-xl;
}

p {
	margin: 0;
}


────────────────────────────────────────────────────────────────────────────────
FILE: src/styles/index.scss
────────────────────────────────────────────────────────────────────────────────

@use 'utils' as *;

@use 'utils/reset' as custom;

@use 'typography';
@use 'global';


────────────────────────────────────────────────────────────────────────────────
FILE: src/styles/utils/_index.scss
────────────────────────────────────────────────────────────────────────────────

@forward 'variables';
@forward 'mixins';


────────────────────────────────────────────────────────────────────────────────
FILE: src/styles/utils/_mixins.scss
────────────────────────────────────────────────────────────────────────────────

@use 'variables' as *;

@mixin flex-header {
	display: flex;
	gap: 124px;
	align-items: center;
}

@mixin flex-around {
	display: flex;
	justify-content: space-around;
	align-items: center;
}

@mixin flex-center($gap: 0) {
	display: flex;
	align-items: center;
	justify-content: center;
	@if $gap != 0 {
		gap: $gap;
	}
}

@mixin flex-align($gap: 0, $justify: flex-start) {
	display: flex;
	align-items: center;
	justify-content: $justify;
	@if $gap != 0 {
		gap: $gap;
	}
}

@mixin flex-between($gap: 0) {
	display: flex;
	align-items: center;
	justify-content: space-between;
	@if $gap != 0 {
		gap: $gap;
	}
}

@mixin flex-around($gap: 0) {
	display: flex;
	align-items: center;
	justify-content: space-around;
	@if $gap != 0 {
		gap: $gap;
	}
}

@mixin flex-column($gap: 0, $align: stretch) {
	display: flex;
	flex-direction: column;
	align-items: $align;
	@if $gap != 0 {
		gap: $gap;
	}
}

@mixin grid-auto($min-width: 250px, $gap: $spacing-lg) {
	display: grid;
	grid-template-columns: repeat(auto-fit, minmax($min-width, 1fr));
	gap: $gap;
}

@mixin grid-columns($columns: 4, $gap: $spacing-lg) {
	display: grid;
	grid-template-columns: repeat($columns, 1fr);
	gap: $gap;
}

@mixin grid-responsive($desktop: 4, $tablet: 2, $mobile: 1, $gap: $spacing-lg) {
	display: grid;
	grid-template-columns: repeat($desktop, 1fr);
	gap: $gap;

	@media (max-width: $breakpoint-tablet) {
		grid-template-columns: repeat($tablet, 1fr);
	}

	@media (max-width: $breakpoint-mobile) {
		grid-template-columns: repeat($mobile, 1fr);
	}
}

@mixin text-ellipsis {
	overflow: hidden;
	white-space: nowrap;
	text-overflow: ellipsis;
}

@mixin text-clamp($lines: 2) {
	display: -webkit-box;
	-webkit-line-clamp: $lines;
	-webkit-box-orient: vertical;
	overflow: hidden;
	text-overflow: ellipsis;
}

@mixin heading($size, $weight: 600, $spacing: 0) {
	font-size: $size;
	font-weight: $weight;
	line-height: 1.2;
	margin: 0;
	@if $spacing != 0 {
		letter-spacing: $spacing;
	}
}

@mixin transition(
	$properties: all,
	$duration: 0.2s,
	$timing: ease,
	$delay: 0s
) {
	transition: $properties $duration $timing $delay;
}

@mixin transitions($transitions...) {
	transition: $transitions;
}

@mixin hover-opacity($opacity: 0.8) {
	cursor: pointer;
	transition: opacity 0.2s ease;

	&:hover {
		opacity: $opacity;
	}
}

@mixin hover-background($color, $opacity: 0.1) {
	cursor: pointer;
	transition: background-color 0.2s ease;
	&:hover {
		color: $color;
		background-color: rgba($color, $opacity);
	}
}

@mixin hover-scale($scale: 1.05) {
	cursor: pointer;
	transition: transform 0.2s ease;

	&:hover {
		transform: scale($scale);
	}
}

@mixin rounded($radius: $radius-sm) {
	border-radius: $radius;
	overflow: hidden;
}

@mixin circle($size) {
	width: $size;
	height: $size;
	border-radius: 50%;
}

@mixin square($size) {
	width: $size;
	height: $size;
}

@mixin absolute-center {
	position: absolute;
	top: 50%;
	left: 50%;
	transform: translate(-50%, -50%);
}

@mixin absolute-vertical-center {
	position: absolute;
	top: 50%;
	transform: translateY(-50%);
}

@mixin cover-parent {
	position: absolute;
	top: 0;
	left: 0;
	right: 0;
	bottom: 0;
}

@mixin focus-outline($color: $color-accent, $offset: 2px, $width: 2px) {
	&:focus-visible {
		outline: $width solid $color;
		outline-offset: $offset;
	}
}

@mixin focus-border($color: $color-accent) {
	&:focus {
		border-color: $color;
		outline: none;
	}
}

@mixin disabled {
	&:disabled,
	&.disabled {
		opacity: 0.5;
		cursor: not-allowed;
		pointer-events: none;
	}
}

@mixin active-state($bg-color, $text-color: null) {
	&.active {
		background-color: $bg-color;
		@if $text-color {
			color: $text-color;
		}
	}
}

@mixin section-spacing($spacing: $spacing-3xl) {
	margin-bottom: $spacing;
}

@mixin padding-xy($x, $y) {
	padding: $y $x;
}

@mixin margin-xy($x, $y) {
	margin: $y $x;
}

@mixin mobile {
	@media (max-width: $breakpoint-mobile) {
		@content;
	}
}

@mixin tablet {
	@media (max-width: $breakpoint-tablet) {
		@content;
	}
}

@mixin desktop {
	@media (min-width: $breakpoint-desktop) {
		@content;
	}
}

@mixin custom-breakpoint($width) {
	@media (max-width: $width) {
		@content;
	}
}

@mixin responsive-image {
	width: 100%;
	height: auto;
	display: block;
	object-fit: cover;
}

@mixin aspect-ratio($width, $height) {
	position: relative;
	overflow: hidden;

	&::before {
		content: '';
		display: block;
		padding-top: calc(($height / $width) * 100%);
	}

	img,
	video {
		@include cover-parent;
		width: 100%;
		height: 100%;
		object-fit: cover;
	}
}

@mixin custom-scrollbar(
	$width: 8px,
	$track-color: transparent,
	$thumb-color: $color-text
) {
	&::-webkit-scrollbar {
		width: $width;
	}

	&::-webkit-scrollbar-track {
		background: $track-color;
	}

	&::-webkit-scrollbar-thumb {
		background: $thumb-color;
		border-radius: $width;

		&:hover {
			background: darken($thumb-color, 10%);
		}
	}
}

@mixin hide-scrollbar {
	scrollbar-width: none;
	-ms-overflow-style: none;

	&::-webkit-scrollbar {
		display: none;
	}
}

@mixin visually-hidden {
	position: absolute;
	width: 1px;
	height: 1px;
	margin: -1px;
	padding: 0;
	overflow: hidden;
	clip: rect(0, 0, 0, 0);
	border: 0;
}

@mixin reset-list {
	list-style: none;
	padding: 0;
	margin: 0;
}

@mixin reset-button {
	background: none;
	border: none;
	padding: 0;
	margin: 0;
	cursor: pointer;
	font: inherit;
	color: inherit;
}

@mixin clearfix {
	&::after {
		content: '';
		display: table;
		clear: both;
	}
}


────────────────────────────────────────────────────────────────────────────────
FILE: src/styles/utils/_reset.scss
────────────────────────────────────────────────────────────────────────────────

@use 'variables' as *;

* {
	margin: 0;
	padding: 0;
	box-sizing: border-box;
}

html {
	font-size: 16px;
}

body {
	font-family: $font-family;
	font-size: $font-size-md;
	line-height: 1.5;
	color: $color-text;
	background-color: $color-secondary;
	-webkit-font-smoothing: antialiased;
	-moz-osx-font-smoothing: grayscale;
}

button,
input,
textarea,
select {
	font-family: inherit;
}

a {
	text-decoration: none;
	color: inherit;
}

ul,
ol {
	list-style: none;
	padding: 0;
	margin: 0;
}

img {
	max-width: 100%;
	display: block;
}

button {
	cursor: pointer;
	border: none;
	background: none;
}


────────────────────────────────────────────────────────────────────────────────
FILE: src/styles/utils/_variables.scss
────────────────────────────────────────────────────────────────────────────────

$color-primary: #080423;
$color-secondary: #ffffff;
$color-accent: #ff3d81;
$color-text: #7a7a7a;
$color-text-lighter: #fff9f9;

$color-hovered-accent: #e73774;

$font-family: 'MyFont', -apple-system, BlinkMacSystemFont, 'Segoe UI',
	sans-serif;

$font-size-sm: 0.875rem; // 14px
$font-size-md: 1rem; // 16px
$font-size-lg: 1.125rem; // 18px
$font-size-xl: 1.875rem; // 30px
$font-size-2xl: 2.5rem; // 40px
$font-size-3xl: 3.75rem; // 60px

$spacing-xs: 0.25rem; // 4px
$spacing-sm: 0.5rem; // 8px
$spacing-2sm: 0.7rem; // 11px
$spacing-md: 1rem; // 16px
$spacing-lg: 1.5rem; // 24px
$spacing-xl: 2rem; // 32px
$spacing-1xl: 2.5rem; // 40px
$spacing-2xl: 3rem; // 48px
$spacing-3xl: 3.75rem; // 60px

$radius-sm: 10px;
$radius-lg: 60px;

$breakpoint-mobile: 500px;
$breakpoint-tablet: 768px;
$breakpoint-desktop: 1024px;


────────────────────────────────────────────────────────────────────────────────
FILE: src/ui/button/Button.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';

const Button = ({
	children,
	variant = 'primary',
	size = 'md',
	type = 'button',
	disabled = false,
	fullWidth = false,
	active = false,
	onClick,
	className = '',
	ariaLabel,
}) => {
	const buttonClasses = [
		styles.button,
		styles[variant],
		styles[size],
		styles[active],
		active && styles.active,
		fullWidth && styles.fullWidth,
		disabled && styles.disabled,
		className,
	]
		.filter(Boolean)
		.join(' ');
	return (
		<button
			type={type}
			className={buttonClasses}
			onClick={onClick}
			disabled={disabled}
			aria-label={ariaLabel}
			aria-pressed={active}
		>
			{children}
		</button>
	);
};

export default Button;


────────────────────────────────────────────────────────────────────────────────
FILE: src/ui/button/styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.button {
	@include flex-center($gap: $spacing-sm);
	@include focus-outline($color-accent);
	@include transition(all);
	font-size: $font-size-md;
	font-weight: 500;
	border: none;
	border-radius: $radius-lg;
	cursor: pointer;

	&.disabled {
		@include disabled;
	}

	&.fullWidth {
		width: 100%;
	}
}

.primary {
	color: $color-secondary;
	background-color: $color-accent;

	&:hover:not(.disabled) {
		background-color: $color-hovered-accent;
	}
}

.secondary {
	color: $color-primary;
	background-color: $color-secondary;
	border: 1px solid $color-text;

	&:hover:not(.disabled) {
		@include hover-background($color-primary, 0.1);
	}

	&.active {
		@include active-state($color-primary, $color-secondary);
		border-color: $color-primary;
	}
}

.outline {
	color: $color-accent;
	background-color: transparent;
	border: 1px solid $color-accent;

	&:hover:not(.disabled) {
		@include hover-background($color-accent, 0.1);
	}
}

.icon {
	padding: $spacing-sm;
	background-color: transparent;

	&:hover:not(.disabled) {
		@include hover-background($color-text, 0.1);
	}
}

.sm {
	@include padding-xy($spacing-md, $spacing-xs);
	font-size: $font-size-sm;
}

.md {
	@include padding-xy($spacing-2xl, $spacing-2sm);
	font-size: $font-size-md;
}

.lg {
	@include padding-xy($spacing-3xl, $spacing-md);
	font-size: $font-size-lg;
}


────────────────────────────────────────────────────────────────────────────────
FILE: tsconfig.json
────────────────────────────────────────────────────────────────────────────────

{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}


────────────────────────────────────────────────────────────────────────────────
FILE: tsconfig.node.json
────────────────────────────────────────────────────────────────────────────────

{
	"compilerOptions": {
		"tsBuildInfoFile": "./node_modules/.tmp/tsconfig.node.tsbuildinfo",
		"target": "ES2023",
		"lib": ["ES2023"],
		"module": "ESNext",
		"types": ["node"],
		"skipLibCheck": true,

		/* Bundler mode */
		"moduleResolution": "bundler",
		"allowImportingTsExtensions": true,
		"verbatimModuleSyntax": true,
		"moduleDetection": "force",
		"noEmit": true,

		/* Linting */
		"strict": true,
		"noUnusedLocals": true,
		"noUnusedParameters": true,
		"erasableSyntaxOnly": true,
		"noFallthroughCasesInSwitch": true,
		"noUncheckedSideEffectImports": true
	},
	"include": ["vite.config.ts"]
}


────────────────────────────────────────────────────────────────────────────────
FILE: vite.config.ts
────────────────────────────────────────────────────────────────────────────────

import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
	plugins: [react()],
	resolve: {
		alias: {
			'@': path.resolve(__dirname, './src'),
			'@styles': path.resolve(__dirname, './src/styles'),
			'@components': path.resolve(__dirname, './src/components'),
			'@pages': path.resolve(__dirname, './src/pages'),
			'@hooks': path.resolve(__dirname, './src/hooks'),
			'@utils': path.resolve(__dirname, './src/utils'),
			'@assets': path.resolve(__dirname, './src/assets'),
			'@redux': path.resolve(__dirname, './src/redux'),
		},
	},
	css: {
		preprocessorOptions: {
			scss: {
				additionalData: `@use "@/styles/utils" as *;`,
			},
		},
	},
});


================================================================================
# END OF PROJECT CONTEXT

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\constants\info.ts
────────────────────────────────────────────────────────────────────────────────

const genres = [
	{ value: 'horror', label: 'Ужасы' },
	{ value: 'action', label: 'Боевик' },
	{ value: 'comedy', label: 'Комедия' },
	{ value: 'drama', label: 'Драма' },
	{ value: 'thriller', label: 'Триллер' },
	{ value: 'fantasy', label: 'Фантастика' },
	{ value: 'detective', label: 'Детектив' },
	{ value: 'romance', label: 'Романтика' },
	{ value: 'adventure', label: 'Приключения' },
	{ value: 'animation', label: 'Мультфильм' },
];

const countries = [
	{ value: 'russia', label: 'Россия' },
	{ value: 'usa', label: 'США' },
	{ value: 'uk', label: 'Великобритания' },
	{ value: 'france', label: 'Франция' },
	{ value: 'germany', label: 'Германия' },
	{ value: 'italy', label: 'Италия' },
	{ value: 'spain', label: 'Испания' },
	{ value: 'japan', label: 'Япония' },
	{ value: 'korea', label: 'Южная Корея' },
	{ value: 'china', label: 'Китай' },
];

export { genres, countries };

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\hooks\useDebounce.ts
────────────────────────────────────────────────────────────────────────────────

import { useEffect, useState } from 'react';

export const useDebounce = (value: string, delay = 500): string => {
	const [debouncedValue, setDebouncedValue] = useState<string>(value);

	useEffect(() => {
		const timerId = setTimeout(() => {
			setDebouncedValue(value);
		}, delay);

		return () => clearTimeout(timerId);
	}, [value, delay]);

	return debouncedValue;
};

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\hooks\useFilteredVideos.ts
────────────────────────────────────────────────────────────────────────────────

import { useGetMoviesByFiltersQuery } from '@/entities/video/api/api';
import type { FilterParams } from '@/entities/video/model/types';

export const useFilteredVideos = (filters: FilterParams) => {
	const { data, isLoading, isError } = useGetMoviesByFiltersQuery(filters);

	if (!data) {
		return {
			items: [],
			isLoading,
			isError,
		};
	}

	return {
		items: data?.items || [],
		total: data?.total || 0,
		totalPages: data?.totalPages || 0,
		isLoading,
		isError,
	};
};

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\hooks\useFrames.ts
────────────────────────────────────────────────────────────────────────────────

import { useGetFramesOfMovieQuery } from '@/entities/video/api/api';

export const useFrames = (id: string) => {
	const { data, isLoading, isError } = useGetFramesOfMovieQuery(id);

	if (!data)
		return {
			items: [],
			isLoading,
			isError,
		};

	return {
		items: data?.items || [],
		total: data?.total || 0,
		totalPages: data?.totalPages || 0,
		isLoading,
		isError,
	};
};

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\hooks\useMovie.ts
────────────────────────────────────────────────────────────────────────────────

import { useGetMovieByIdQuery } from '@/entities/video/api/api';

export const useMovie = (id: string) => {
	const { data, isLoading, isError } = useGetMovieByIdQuery(id);

	if (!data) {
		return {
			video: {
				filmId: 0,
				kinopoiskId: 0,
				imdbId: '',
				nameRu: '',
				nameEn: '',
				nameOriginal: '',
				posterUrl: '',
				posterUrlPreview: '',
				coverUrl: '',
				logoUrl: '',
				ratingKinopoisk: 0,
				ratingImdb: 0,
				description: '',
				shortDescription: '',
				countries: [],
				genres: [],
				year: 0,
				type: '',
			},
			isLoading: false,
			isError: true,
		};
	}

	return { video: data, isLoading, isError };
};

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\hooks\useSearch.ts
────────────────────────────────────────────────────────────────────────────────

import { useGetMovieBySearchQuery } from '@/entities/video/api/api';

export const useSearch = (keyword: string) => {
	const { data, isLoading, isError } = useGetMovieBySearchQuery(keyword);

	if (!data) {
		return {
			films: [],
			keyword: '',
			pagesCount: 0,
			searchFilmsCountResult: 0,
			isLoading,
			isError,
		};
	}

	return {
		keyword: data.keyword,
		pagesCount: data.pagesCount,
		searchFilmsCountResult: data.searchFilmsCountResult,
		films: data.films,
		isLoading,
		isError,
	};
};

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\hooks\useSimilar.ts
────────────────────────────────────────────────────────────────────────────────

import { useGetSimilarMoviesQuery } from '@/entities/video/api/api';

export const useSimilar = (id: string) => {
	const { data, isLoading, isError } = useGetSimilarMoviesQuery(id);

	if (!data) {
		return {
			items: [],
			isLoading,
			isError,
		};
	}

	return {
		items: data?.items || [],
		total: data?.total || 0,
		isLoading,
		isError,
	};
};

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\hooks\useStaff.ts
────────────────────────────────────────────────────────────────────────────────

import { useGetStaffByIdQuery } from '@/entities/video';

export const useStaff = (id: string) => {
	const { data, isLoading, isError } = useGetStaffByIdQuery(id);

	return {
		staff: data || [],
		isLoading,
		isError,
	};
};

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\hooks\useVideos.ts
────────────────────────────────────────────────────────────────────────────────

import type { VideoCategory } from '@/entities/video';
import { useGetMoviesByCategoryQuery } from '@/entities/video/api/api';

export const useVideos = (category: VideoCategory = 'TOP_POPULAR_MOVIES') => {
	const { data, isLoading, isError } = useGetMoviesByCategoryQuery(category);

	if (!data) {
		return {
			items: [],
			isLoading,
			isError,
		};
	}

	return {
		items: data?.items || [],
		total: data?.total || 0,
		totalPages: data?.totalPages || 0,
		isLoading,
		isError,
	};
};

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\styles\utils\_index.scss
────────────────────────────────────────────────────────────────────────────────

@forward 'variables';
@forward 'mixins';

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\styles\utils\_mixins.scss
────────────────────────────────────────────────────────────────────────────────

@use 'variables' as *;

@mixin skeleton-pulse {
	background: linear-gradient(-90deg, #f0f0f0 0%, #f8f8f8 50%, #f0f0f0 100%);
	background-size: 400% 400%;
	animation: pulse 1.2s ease-in-out infinite;
}

@keyframes pulse {
	0% {
		background-position: 0% 0%;
	}
	100% {
		background-position: -135% 0%;
	}
}

@mixin flex-header {
	display: flex;
	gap: 124px;
	align-items: center;
}

@mixin flex-around {
	display: flex;
	justify-content: space-around;
	align-items: center;
}

@mixin flex-center($gap: 0) {
	display: flex;
	align-items: center;
	justify-content: center;
	@if $gap != 0 {
		gap: $gap;
	}
}

@mixin flex-align($gap: 0, $justify: flex-start) {
	display: flex;
	align-items: center;
	justify-content: $justify;
	@if $gap != 0 {
		gap: $gap;
	}
}

@mixin flex-between($gap: 0) {
	display: flex;
	align-items: center;
	justify-content: space-between;
	@if $gap != 0 {
		gap: $gap;
	}
}

@mixin flex-around($gap: 0) {
	display: flex;
	align-items: center;
	justify-content: space-around;
	@if $gap != 0 {
		gap: $gap;
	}
}

@mixin flex-column($gap: 0, $align: stretch) {
	display: flex;
	flex-direction: column;
	align-items: $align;
	@if $gap != 0 {
		gap: $gap;
	}
}

@mixin grid-auto($min-width: 250px, $gap: $spacing-lg) {
	display: grid;
	grid-template-columns: repeat(auto-fit, minmax($min-width, 1fr));
	gap: $gap;
}

@mixin grid-columns($columns: 4, $gap: $spacing-lg) {
	display: grid;
	grid-template-columns: repeat($columns, 1fr);
	gap: $gap;
}

@mixin grid-responsive($desktop: 4, $tablet: 2, $mobile: 1, $gap: $spacing-lg) {
	display: grid;
	grid-template-columns: repeat($desktop, 1fr);
	gap: $gap;

	@media (max-width: $breakpoint-tablet) {
		grid-template-columns: repeat($tablet, 1fr);
	}

	@media (max-width: $breakpoint-mobile) {
		grid-template-columns: repeat($mobile, 1fr);
	}
}

@mixin text-ellipsis {
	overflow: hidden;
	white-space: nowrap;
	text-overflow: ellipsis;
}

@mixin text-clamp($lines: 2) {
	display: -webkit-box;
	-webkit-line-clamp: $lines;
	-webkit-box-orient: vertical;
	overflow: hidden;
	text-overflow: ellipsis;
}

@mixin heading($size, $weight: 600, $spacing: 0) {
	font-size: $size;
	font-weight: $weight;
	line-height: 1.2;
	margin: 0;
	@if $spacing != 0 {
		letter-spacing: $spacing;
	}
}

@mixin transition(
	$properties: all,
	$duration: 0.2s,
	$timing: ease,
	$delay: 0s
) {
	transition: $properties $duration $timing $delay;
}

@mixin transitions($transitions...) {
	transition: $transitions;
}

@mixin hover-opacity($opacity: 0.8) {
	cursor: pointer;
	transition: opacity 0.2s ease;

	&:hover {
		opacity: $opacity;
	}
}

@mixin hover-background($color, $opacity: 0.1) {
	cursor: pointer;
	transition: background-color 0.2s ease;
	&:hover {
		color: $color;
		background-color: rgba($color, $opacity);
	}
}

@mixin hover-scale($scale: 1.05) {
	cursor: pointer;
	transition: transform 0.2s ease;

	&:hover {
		transform: scale($scale);
	}
}

@mixin rounded($radius: $radius-sm) {
	border-radius: $radius;
	overflow: hidden;
}

@mixin circle($size) {
	width: $size;
	height: $size;
	border-radius: 50%;
}

@mixin square($size) {
	width: $size;
	height: $size;
}

@mixin absolute-center {
	position: absolute;
	top: 50%;
	left: 50%;
	transform: translate(-50%, -50%);
}

@mixin absolute-vertical-center {
	position: absolute;
	top: 50%;
	transform: translateY(-50%);
}

@mixin cover-parent {
	position: absolute;
	top: 0;
	left: 0;
	right: 0;
	bottom: 0;
}

@mixin focus-outline($color: $color-accent, $offset: 2px, $width: 2px) {
	&:focus-visible {
		outline: $width solid $color;
		outline-offset: $offset;
	}
}

@mixin focus-border($color: $color-accent) {
	&:focus {
		border-color: $color;
		outline: none;
	}
}

@mixin disabled {
	&:disabled,
	&.disabled {
		opacity: 0.5;
		cursor: not-allowed;
		pointer-events: none;
	}
}

@mixin active-state($bg-color, $text-color: null) {
	&.active {
		background-color: $bg-color;
		@if $text-color {
			color: $text-color;
		}
	}
}

@mixin section-spacing($spacing: $spacing-3xl) {
	margin-bottom: $spacing;
}

@mixin padding-xy($x, $y) {
	padding: $y $x;
}

@mixin margin-xy($x, $y) {
	margin: $y $x;
}

@mixin mobile {
	@media (max-width: $breakpoint-mobile) {
		@content;
	}
}
@mixin tablet {
	@media (max-width: $breakpoint-tablet) {
		@content;
	}
}

@mixin desktop {
	@media (min-width: $breakpoint-desktop) {
		@content;
	}
}

@mixin custom-breakpoint($width) {
	@media (max-width: $width) {
		@content;
	}
}

@mixin responsive-image {
	width: 100%;
	height: auto;
	display: block;
	object-fit: cover;
}

@mixin aspect-ratio($width, $height) {
	position: relative;
	overflow: hidden;

	&::before {
		content: '';
		display: block;
		padding-top: calc(($height / $width) * 100%);
	}

	img,
	video {
		@include cover-parent;
		width: 100%;
		height: 100%;
		object-fit: cover;
	}
}

@mixin custom-scrollbar(
	$width: 8px,
	$track-color: transparent,
	$thumb-color: $color-text
) {
	&::-webkit-scrollbar {
		width: $width;
	}

	&::-webkit-scrollbar-track {
		background: $track-color;
	}

	&::-webkit-scrollbar-thumb {
		background: $thumb-color;
		border-radius: $width;

		&:hover {
			background: darken($thumb-color, 10%);
		}
	}
}

@mixin hide-scrollbar {
	scrollbar-width: none;
	-ms-overflow-style: none;

	&::-webkit-scrollbar {
		display: none;
	}
}

@mixin visually-hidden {
	position: absolute;
	width: 1px;
	height: 1px;
	margin: -1px;
	padding: 0;
	overflow: hidden;
	clip: rect(0, 0, 0, 0);
	border: 0;
}

@mixin reset-list {
	list-style: none;
	padding: 0;
	margin: 0;
}

@mixin reset-button {
	background: none;
	border: none;
	padding: 0;
	margin: 0;
	cursor: pointer;
	font: inherit;
	color: inherit;
}

@mixin clearfix {
	&::after {
		content: '';
		display: table;
		clear: both;
	}
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\styles\utils\_reset.scss
────────────────────────────────────────────────────────────────────────────────

@use 'variables' as *;

* {
	margin: 0;
	padding: 0;
	box-sizing: border-box;
}

html {
	font-size: 16px;
}

body {
	font-family: $font-family;
	font-size: $font-size-md;
	line-height: 1.5;
	color: $color-text;
	background-color: $color-secondary;
	-webkit-font-smoothing: antialiased;
	-moz-osx-font-smoothing: grayscale;
}

button,
input,
textarea,
select {
	font-family: inherit;
}

a {
	text-decoration: none;
	color: inherit;
}

ul,
ol {
	list-style: none;
	padding: 0;
	margin: 0;
}

img {
	max-width: 100%;
	display: block;
}

button {
	cursor: pointer;
	border: none;
	background: none;
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\styles\utils\_variables.scss
────────────────────────────────────────────────────────────────────────────────

$color-primary: #080423;
$color-secondary: #ffffff;
$color-accent: #ff3d81;
$color-text: #7a7a7a;
$color-text-lighter: #fff9f9;

$color-hovered-accent: #e73774;

$font-family: 'MyFont', -apple-system, BlinkMacSystemFont, 'Segoe UI',
	sans-serif;

$font-size-sm: 0.875rem; 
$font-size-md: 1rem; 
$font-size-lg: 1.125rem; 
$font-size-xl: 1.875rem; 
$font-size-2xl: 2.5rem; 
$font-size-3xl: 3.75rem; 

$spacing-xs: 0.25rem; 
$spacing-sm: 0.5rem; 
$spacing-2sm: 0.7rem; 
$spacing-md: 1rem; 
$spacing-lg: 1.5rem; 
$spacing-xl: 2rem; 
$spacing-1xl: 2.5rem; 
$spacing-2xl: 3rem; 
$spacing-3xl: 3.75rem; 

$radius-sm: 10px;
$radius-lg: 60px;

$breakpoint-mobile: 500px;
$breakpoint-tablet: 768px;
$breakpoint-desktop: 1024px;

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\styles\_global.scss
────────────────────────────────────────────────────────────────────────────────

@use './utils' as *;
.container {
	display: flex;
	flex-direction: column;
	min-height: 100vh;
	margin: 0 auto;
	padding: $spacing-3xl;
	flex: 1 0 auto;
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\styles\_typography.scss
────────────────────────────────────────────────────────────────────────────────

@use './utils/variables' as *;

@font-face {
	font-family: 'MyFont';
	src: url('@shared/assets/fonts/MyFont.ttf') format('truetype');
	font-weight: 400;
	font-style: normal;
	font-display: swap;
}

@font-face {
	font-family: 'Bayon';
	src: url('@shared/assets/fonts/Bayon.ttf') format('truetype');
	font-weight: 300;
	font-style: normal;
	font-display: swap;
}

h1,
h2,
h3,
h4,
h5,
h6 {
	margin: 0;
	font-weight: 600;
	line-height: 1.2;
}

h1 {
	font-size: $font-size-3xl;
}

h2 {
	font-size: $font-size-2xl;
}

h3 {
	font-size: $font-size-xl;
}

p {
	margin: 0;
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\types\types.ts
────────────────────────────────────────────────────────────────────────────────

export type SkeletonType = 'banner' | 'item';
export type DirectionType = 'next' | 'back';
export type SeparatorType = 'next' | 'back' | 'up' | 'down' | 'select';
export interface Option {
	value: string;
	label: string;
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\ui\Button\Button.tsx
────────────────────────────────────────────────────────────────────────────────

import type { ReactNode } from 'react';
import styles from './styles.module.scss';

interface Props {
	children?: ReactNode;
	variant?: string;
	size?: string;
	type?: 'button' | 'submit' | 'reset';
	disabled?: boolean;
	active?: boolean;
	onClick: () => void;
	className?: string;
	ariaLabel?: string;
}

const Button = ({
	children,
	variant = 'primary',
	size = 'md',
	type = 'button',
	disabled = false,
	active,
	onClick,
	className = '',
	ariaLabel,
}: Props) => {
	const buttonClasses = [
		styles.button,
		styles[variant],
		styles[size],
		active && styles.active,
		disabled && styles.disabled,
		className,
	]
		.filter(Boolean)
		.join(' ');
	return (
		<button
			type={type}
			className={buttonClasses}
			onClick={onClick}
			disabled={disabled}
			aria-label={ariaLabel}
			aria-pressed={active}
		>
			{children}
		</button>
	);
};

export default Button;

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\ui\Button\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.button {
	@include flex-center($gap: $spacing-sm);
	@include focus-outline($color-accent);
	@include transition(all);
	font-size: $font-size-md;
	font-weight: 500;
	border: none;
	border-radius: $radius-lg;
	cursor: pointer;

	&.disabled {
		@include disabled;
	}

	&.fullWidth {
		width: 100%;
	}
}

.primary {
	color: $color-secondary;
	background-color: $color-accent;

	&:hover:not(.disabled) {
		background-color: $color-hovered-accent;
	}
}

.secondary {
	color: $color-primary;
	background-color: $color-secondary;
	border: 1px solid $color-text;

	&:hover:not(.disabled):not(.active) {
		@include hover-background($color-primary, 0.1);
	}

	&.active {
		background-color: $color-primary;
		color: $color-secondary;
	}
}

.outline {
	color: $color-secondary;
	background-color: $color-primary;
	border: 1px solid $color-primary;

	&:hover:not(.disabled) {
		@include hover-background($color-primary, 0.1);
	}
}

.icon {
	padding: $spacing-sm;
	background-color: transparent;

	&:hover:not(.disabled) {
		@include hover-background($color-text, 0.1);
	}
}

.sm {
	@include padding-xy($spacing-md, $spacing-xs);
	font-size: $font-size-sm;
}

.md {
	@include padding-xy($spacing-2xl, $spacing-2sm);
	font-size: $font-size-md;
}

.lg {
	@include padding-xy($spacing-3xl, $spacing-md);
	font-size: $font-size-lg;
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\ui\Image\Image.tsx
────────────────────────────────────────────────────────────────────────────────

import { useState } from 'react';
import styles from './styles.module.scss';
import ImageSkeleton from './ImageSkeleton';
interface Props {
	src: string;
	alt: string;
	type?: 'poster' | 'banner' | 'frame';
}

const Image = ({ src, alt = '', type = 'poster' }: Props) => {
	const [hasError, setHasError] = useState(false);

	if (!src || hasError) {
		return <ImageSkeleton />;
	}

	return (
		<img
			src={src}
			alt={alt}
			className={
				type === 'banner'
					? styles.banner
					: type === 'frame'
					? styles.frame
					: styles.poster
			}
			onError={() => setHasError(true)}
		/>
	);
};

export default Image;

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\ui\Image\ImageSkeleton.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';

const ImageSkeleton = () => {
	return <div className={styles.imageSkeleton} />;
};

export default ImageSkeleton;

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\ui\Image\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.poster {
	object-fit: cover;
	border-radius: $radius-sm;
}

.imageSkeleton {
	@include skeleton-pulse;
	border-radius: $radius-sm;
	width: 355px;
	height: 500px;
}

.banner {
	object-fit: cover;
	border-radius: $radius-sm;
	width: 765px;
	height: 500px;
}

.frame {
	width: 430px;
	height: 420px;
	border-radius: $radius-sm;
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\ui\Logo\Logo.tsx
────────────────────────────────────────────────────────────────────────────────

import { Link } from 'react-router-dom';
import styles from './styles.module.scss';

const Logo = () => {
	return (
		<Link to={'/'}>
			<h2 className={styles.logo}>kinomonster</h2>
		</Link>
	);
};

export default Logo;

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\ui\Logo\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.logo {
	font-family: 'Bayon';
	letter-spacing: 5px;
	color: $color-accent;
	text-transform: uppercase;
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\ui\Rating\Rating.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';
import type { ReactNode } from 'react';
interface Props {
	children: ReactNode;
}

const Rating = ({ children }: Props) => {
	return (
		<span className={styles.rating} aria-label='Рейтинг'>
			⭐{children ? children : 0}/10
		</span>
	);
};

export default Rating;

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\ui\Rating\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.rating {
	font-size: $font-size-sm;
	align-self: flex-end;
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\ui\Select\Select.tsx
────────────────────────────────────────────────────────────────────────────────

import type { ChangeEvent } from 'react';
import styles from './styles.module.scss';
import type { Option } from '@/shared/types/types';
interface Props {
	options: Option[];
	value: string;
	onChange: (value: string) => void;
}

const Select = ({ options, value, onChange }: Props) => {
	const handleSelectChange = (e: ChangeEvent<HTMLSelectElement>) => {
		onChange(e.target.value);
	};
	return (
		<select
			className={styles.select}
			value={value}
			onChange={handleSelectChange}
		>
			{options?.map(option => (
				<option
					className={styles.option}
					key={option.value}
					value={option.value}
				>
					{option.label}
				</option>
			))}
		</select>
	);
};

export default Select;

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\ui\Select\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.select {
	appearance: none;
	@include rounded($radius-lg);
	@include padding-xy($spacing-md, $spacing-2sm);
	@include focus-border(rgba($color-text, 0.5));
	@include transition(border-color);
	padding-right: $spacing-xl;
	color: $color-text;
	border-color: $color-text;
	background-color: transparent;
	background-image: url('@shared/assets/icons/arrowSelect.svg');
	background-repeat: no-repeat;
	background-position: right $spacing-sm center;
	background-size: 16px;

	&:hover {
		cursor: pointer;
	}
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\ui\Separator\Separator.tsx
────────────────────────────────────────────────────────────────────────────────

import arrowLeft from '@shared/assets/icons/arrowLeft.svg';
import arrowRight from '@shared/assets/icons/arrowRight.svg';
import arrowDown from '@shared/assets/icons/arrowDown.svg';
import arrowUp from '@shared/assets/icons/arrowUp.svg';
import arrowSelect from '@shared/assets/icons/arrowSelect.svg';
import type { SeparatorType } from '@/shared/types/types';
interface Props {
	direction: SeparatorType;
}

const Separator = ({ direction }: Props) => {
	const icons = {
		back: arrowLeft,
		next: arrowRight,
		up: arrowUp,
		down: arrowDown,
		select: arrowSelect,
	};
	return <img src={icons[direction]} alt={direction} />;
};

export default Separator;

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\ui\Tab\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.tab {
	background-color: $color-secondary;
	border-radius: $radius-lg;
	color: $color-primary;
	padding: $spacing-2sm $spacing-xl;
	font-size: $font-size-md;

	&.active {
		background-color: $color-primary;
		color: $color-secondary;
	}
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\ui\Tab\Tab.tsx
────────────────────────────────────────────────────────────────────────────────

import type { ReactNode } from 'react';

interface TabProps {
	label: string;
	children?: ReactNode;
	onClick?: () => void;
}

const Tab = ({ children }: TabProps) => {
	return <>{children}</>;
};

export default Tab;

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\ui\Tabs\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.container {
	@include flex-column($gap: $spacing-xl);
}

.tabs {
	display: flex;
	gap: $spacing-lg;
	margin-bottom: $spacing-xl;
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\ui\Tabs\Tabs.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';
import { useState, type ReactNode } from 'react';
import Button from '../Button/Button';

interface Props {
	children: ReactNode;
	defaultActiveIndex: number;
}

const Tabs = ({ children, defaultActiveIndex = 0 }: Props) => {
	const [activeIndex, setActiveIndex] = useState(defaultActiveIndex);
	const tabs = Array.isArray(children) ? children : [children];

	const handleTabClick = (index: number) => {
		const tab = tabs[index];
		setActiveIndex(index);
		tab.props.onClick?.();
	};

	return (
		<div className={styles.container}>
			<div className={styles.tabs}>
				{tabs.map((tab, index) => (
					<Button
						key={index}
						onClick={() => handleTabClick(index)}
						variant='secondary'
						active={activeIndex === index}
					>
						{tab.props.label}
					</Button>
				))}
			</div>

			<div className={styles.content}>{tabs[activeIndex]}</div>
		</div>
	);
};

export default Tabs;

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\ui\Title\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.title {
	@include text-clamp(2);
	@include transition(color);
	white-space: normal;
	font-weight: 600;
	color: $color-secondary;
	background-color: $color-primary;
	border-radius: $radius-lg;
	margin-bottom: $spacing-1xl;
	padding: $spacing-2sm $spacing-xl;
	align-self: center;
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\ui\Title\Title.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';
import type { ReactNode } from 'react';
interface Props {
	children: ReactNode;
}

const Title = ({ children }: Props) => {
	return <h3 className={styles.title}>{children}</h3>;
};

export default Title;

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\ui\Year\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.year {
	flex: 1;
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\shared\ui\Year\Year.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';
import type { ReactNode } from 'react';
interface Props {
	children: ReactNode;
}

const Year = ({ children }: Props) => {
	return <p className={styles.year}>{children}</p>;
};

export default Year;

────────────────────────────────────────────────────────────────────────────────

FILE: src\widgets\contentCatalog\ContentCatalog.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';
import { Breadcrumbs } from '@/features';
import Tab from '@/shared/ui/Tab/Tab';
import Tabs from '@/shared/ui/Tabs/Tabs';
import { useState } from 'react';
import { useVideos } from '@/shared/hooks/useVideos';
import { VideoList, type VideoCategory } from '@/entities/video';
import VideoListSkeleton from '@/entities/video/ui/VideoList/VideoListSkeleton';

const ContentCatalog = () => {
	const [activeCategory, setActiveCategory] = useState('TOP_POPULAR_MOVIES');
	const { items, isLoading } = useVideos(activeCategory as VideoCategory);

	if (isLoading) return <VideoListSkeleton count={12} />;

	return (
		<section className={styles.content}>
			<Tabs defaultActiveIndex={0}>
				<Tab
					label='Популярные фильмы'
					onClick={() => setActiveCategory('TOP_POPULAR_MOVIES')}
				/>
				<Tab
					label='Популярные сериалы'
					onClick={() => setActiveCategory('POPULAR_SERIES')}
				/>
				<Tab
					label='Подборка фильмов'
					onClick={() => setActiveCategory('TOP_250_MOVIES')}
				/>
			</Tabs>

			<Breadcrumbs to={'/movie'} direction='next'>
				Смотреть все
			</Breadcrumbs>
			<VideoList items={items} limit={12} />
		</section>
	);
};

export default ContentCatalog;

────────────────────────────────────────────────────────────────────────────────

FILE: src\widgets\contentCatalog\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.content {
	@include section-spacing(calc($spacing-3xl + $spacing-1xl));
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\widgets\error\index.ts
────────────────────────────────────────────────────────────────────────────────

export { default as ErrorBoundary } from './ui/ErrorBoundary/ErrorBoundary';
export { default as ErrorFallback } from './ui/ErrorFallback/ErrorFallback';

────────────────────────────────────────────────────────────────────────────────

FILE: src\widgets\error\ui\ErrorBoundary\ErrorBoundary.tsx
────────────────────────────────────────────────────────────────────────────────

import React from 'react';
import ErrorFallback from '../ErrorFallback/ErrorFallback';

interface ErrorBoundaryState {
	hasError: boolean;
	error?: Error;
}

class ErrorBoundary extends React.Component<
	React.PropsWithChildren<{
		fallback?: React.ComponentType<{ error?: Error }>;
	}>,
	ErrorBoundaryState
> {
	constructor(props: any) {
		super(props);
		this.state = { hasError: false };
	}

	static getDerivedStateFromError(error: Error): ErrorBoundaryState {
		return { hasError: true, error };
	}

	componentDidCatch(error: Error, errorInfo: React.ErrorInfo) {
		console.error('ErrorBoundary caught an error:', error, errorInfo);
	}

	resetError = () => {
		this.setState({ hasError: false, error: undefined });
	};

	render() {
		if (this.state.hasError) {
			const FallbackComponent = this.props.fallback || ErrorFallback;
			return (
				<FallbackComponent
					error={this.state.error}
					resetError={this.resetError}
				/>
			);
		}

		return this.props.children;
	}
}

export default ErrorBoundary;

────────────────────────────────────────────────────────────────────────────────

FILE: src\widgets\error\ui\ErrorFallback\ErrorFallback.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';
import Button from '@/shared/ui/Button/Button';
import { useNavigate } from 'react-router-dom';

interface ErrorFallbackProps {
	error?: Error;
	resetError?: () => void;
}

const ErrorFallback = ({ error, resetError }: ErrorFallbackProps) => {
	const navigate = useNavigate();

	const handleGoHome = () => {
		navigate('/');
		resetError?.();
	};

	const handleReload = () => {
		window.location.reload();
	};

	return (
		<div className={styles.container}>
			<div className={styles.content}>
				<h1 className={styles.title}>Что-то пошло не так</h1>
				<p className={styles.message}>
					Произошла непредвиденная ошибка. Пожалуйста, попробуйте еще раз.
				</p>

				{error && (
					<details className={styles.details}>
						<summary>Подробности ошибки</summary>
						<pre className={styles.error}>{error.message}</pre>
					</details>
				)}

				<div className={styles.actions}>
					<Button onClick={handleReload} ariaLabel='Перезагрузить страницу'>
						Перезагрузить
					</Button>
					<Button
						onClick={handleGoHome}
						variant='outline'
						ariaLabel='Вернуться на главную'
					>
						На главную
					</Button>
				</div>
			</div>
		</div>
	);
};

export default ErrorFallback;

────────────────────────────────────────────────────────────────────────────────

FILE: src\widgets\error\ui\ErrorFallback\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.container {
	@include flex-center;
	min-height: 60vh;
	padding: $spacing-3xl;
}

.content {
	text-align: center;
	max-width: 500px;
}

.title {
	font-size: $font-size-2xl;
	color: $color-primary;
	margin-bottom: $spacing-lg;
}

.message {
	color: $color-text;
	margin-bottom: $spacing-2xl;
	line-height: 1.6;
}

.details {
	margin-bottom: $spacing-2xl;
	text-align: left;

	summary {
		cursor: pointer;
		color: $color-accent;
		margin-bottom: $spacing-sm;
	}
}

.error {
	background: rgba($color-primary, 0.05);
	padding: $spacing-md;
	border-radius: $radius-sm;
	font-size: $font-size-sm;
	color: $color-text;
	overflow-x: auto;
	white-space: pre-wrap;
}

.actions {
	@include flex-center($gap: $spacing-md);
	flex-wrap: wrap;
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\widgets\footer\Footer.tsx
────────────────────────────────────────────────────────────────────────────────

import Logo from '@/shared/ui/Logo/Logo';
import styles from './styles.module.scss';
import Navigation from '@/widgets/navigation/Navigation';

const Footer = () => {
	return (
		<footer className={styles.footer}>
			<Logo />
			<Navigation />
		</footer>
	);
};

export default Footer;

────────────────────────────────────────────────────────────────────────────────

FILE: src\widgets\footer\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.footer {
	@include flex-between;
	padding: $spacing-2xl;
	background-color: $color-primary;
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\widgets\header\Header.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';
import Logo from '@/shared/ui/Logo/Logo';
import Search from '@/features/searchVideo/ui/SearchVideo/SearchVideo';

const Header = () => {
	return (
		<header className={styles.header}>
			<Logo />
			<Search />
		</header>
	);
};

export default Header;

────────────────────────────────────────────────────────────────────────────────

FILE: src\widgets\header\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.header {
	@include flex-align($gap: calc($spacing-3xl * 2 + $spacing-xs));
	@include padding-xy(calc($spacing-2xl * 2), $spacing-md);
	font-weight: 400;
	background-color: $color-primary;
	color: $color-accent;
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\widgets\heroBanner\HeroBanner.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';
import VideoHero from '@/entities/video/ui/VideoHero/VideoHero';
import Image from '@/shared/ui/Image/Image';
import { useVideos } from '@/shared/hooks/useVideos';
import HeroBannerSkeleton from './HeroBannerSkeleton';

const HeroBanner = () => {
	const { items, isLoading } = useVideos('TOP_POPULAR_MOVIES');

	const data = items[0];

	if (isLoading) return <HeroBannerSkeleton />;

	return (
		<section className={styles.banner}>
			<VideoHero
				id={data.kinopoiskId}
				title={data.nameRu}
				description={
					data.shortDescription ? data.shortDescription : data.description
				}
			/>
			<Image src={data.posterUrl} alt={'banner'} type='banner' />
		</section>
	);
};

export default HeroBanner;

────────────────────────────────────────────────────────────────────────────────

FILE: src\widgets\heroBanner\HeroBannerSkeleton.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';

const HeroBannerSkeleton = () => {
	return (
		<section className={styles.bannerSkeleton}>
			<div className={styles.videoHero}>
				<div className={styles.statusSkeleton} />
				<div className={styles.titleSkeleton} />
				<div className={styles.descriptionSkeleton} />
				<div className={styles.buttonSkeleton} />
			</div>
			<div className={styles.image} />
		</section>
	);
};

export default HeroBannerSkeleton;

────────────────────────────────────────────────────────────────────────────────

FILE: src\widgets\heroBanner\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.banner,
.bannerSkeleton {
	@include flex-align($gap: $spacing-1xl);
	padding: $spacing-xl;
	border-radius: $radius-sm;
	background: #0c0a2a;
	@include section-spacing(calc($spacing-3xl + $spacing-1xl));

	@include tablet {
		flex-direction: column;
		align-items: flex-start;
	}
}

.videoHero {
	flex: 1;
	min-height: 300px;
}

.image {
	width: 765px;
	height: 500px;
	border-radius: $radius-sm;
}

.videoHero,
.image {
	@include skeleton-pulse;
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\widgets\index.ts
────────────────────────────────────────────────────────────────────────────────

import ContentCatalog from './contentCatalog/ContentCatalog';
import Footer from './footer/Footer';
import Header from './header/Header';
import HeroBanner from './heroBanner/HeroBanner';
import Navigation from './navigation/Navigation';
import VideoFilters from './videoFilters/VideoFilters';
export { ErrorBoundary, ErrorFallback } from './error';

export { ContentCatalog, Footer, Header, HeroBanner, Navigation, VideoFilters };

────────────────────────────────────────────────────────────────────────────────

FILE: src\widgets\navigation\Navigation.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';
import { Link } from 'react-router-dom';

const Navigation = () => {
	return (
		<nav className={styles.navigation}>
			<Link to={'/'}>Главная</Link>
			<Link to={'/popular-movies'}>Популярные фильмы</Link>
			<Link to={'/popular-series'}>Популярные сериалы</Link>
		</nav>
	);
};

export default Navigation;

────────────────────────────────────────────────────────────────────────────────

FILE: src\widgets\navigation\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.navigation {
	@include flex-align($gap: $spacing-1xl);
	color: $color-text-lighter;

	a {
		@include transition(color);

		&:hover {
			color: $color-accent;
		}
	}
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\widgets\searchResults\SearchResults.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';
import { useSearch } from '@/shared/hooks/useSearch';
import { useSearchParams } from 'react-router';
import VideoListSkeleton from '@/entities/video/ui/VideoList/VideoListSkeleton';
import SearchVideoCard from '../searchVideoCard/SearchVideoCard';

const SearchResults = () => {
	const [params] = useSearchParams();
	const query = params.get('query') ?? '';
	const { films, isLoading } = useSearch(query);

	if (isLoading) return <VideoListSkeleton count={12} />;
	if (!films.length) return <div>not found</div>;

	return (
		<ul className={styles.list}>
			{films.map(video => (
				<SearchVideoCard key={video.filmId} video={video} />
			))}
		</ul>
	);
};

export default SearchResults;

────────────────────────────────────────────────────────────────────────────────

FILE: src\widgets\searchResults\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.list {
	display: flex;
	flex-direction: column;
	gap: $spacing-1xl;
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\widgets\searchVideoCard\SearchVideoCard.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';
import Image from '@/shared/ui/Image/Image';
import { VideoDetails, VideoFacts, type Video } from '@/entities/video';
import { useStaff } from '@/shared/hooks/useStaff';

interface Props {
	video: Video;
}

const SearchVideoCard = ({ video }: Props) => {
	const id = video.filmId.toString();
	const { staff } = useStaff(id);

	return (
		<div className={styles.searchCard}>
			<Image src={video.posterUrlPreview} alt={video.nameRu} />
			<div className={styles.info}>
				<VideoDetails
					id={id}
					title={video.nameRu}
					description={video.description}
				/>
				<VideoFacts video={video} staff={staff} />
			</div>
		</div>
	);
};

export default SearchVideoCard;

────────────────────────────────────────────────────────────────────────────────

FILE: src\widgets\searchVideoCard\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.searchCard {
	display: flex;
	gap: $spacing-2xl;
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\widgets\videoDescription\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.info,
.infoSkeleton {
	display: flex;
	gap: $spacing-2xl;
	@include section-spacing(calc($spacing-3xl + $spacing-lg));
}

.detailsSkeleton {
	display: flex;
	flex-direction: column;
	gap: 30px;
}

.imageSkeleton {
	width: 100%;
	max-width: 482px;
	aspect-ratio: 482 / 650;
}

.movieNameSkeleton,
.btnsSkeleton {
	width: 316px;
	height: 95px;
}

.descriptionSkeleton {
	width: 482px;
	height: 400px;
}

.imageSkeleton,
.descriptionSkeleton,
.movieNameSkeleton,
.btnsSkeleton {
	@include skeleton-pulse;
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\widgets\videoDescription\VideoDescription.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';
import Image from '@/shared/ui/Image/Image';
import VideoDescriptionSkeleton from './VideoDescriptionSkeleton';
import { useStaff } from '@/shared/hooks/useStaff';
import { useMovie } from '@/shared/hooks/useMovie';
import { VideoDetails, VideoFacts } from '@/entities/video';
interface Props {
	filmId: string;
}

const VideoDescription = ({ filmId }: Props) => {
	const { video, isLoading: videoLoading } = useMovie(filmId);
	const { staff, isLoading: staffLoading } = useStaff(filmId);

	if (videoLoading || staffLoading) return <VideoDescriptionSkeleton />;

	return (
		<section className={styles.info}>
			<Image src={video.posterUrl} alt={video.nameRu} />
			<article className={styles.description}>
				<VideoDetails
					id={filmId}
					title={video.nameRu}
					description={video.description}
				/>
				<VideoFacts video={video} staff={staff} />
			</article>
		</section>
	);
};

export default VideoDescription;

────────────────────────────────────────────────────────────────────────────────

FILE: src\widgets\videoDescription\VideoDescriptionSkeleton.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';

const VideoDescriptionSkeleton = () => {
	return (
		<section className={styles.infoSkeleton}>
			<div className={styles.imageSkeleton}></div>
			<div className={styles.detailsSkeleton}>
				<h1 className={styles.movieNameSkeleton}></h1>
				<p className={styles.descriptionSkeleton}></p>
				<div className={styles.btnsSkeleton}></div>
			</div>
		</section>
	);
};

export default VideoDescriptionSkeleton;

────────────────────────────────────────────────────────────────────────────────

FILE: src\widgets\videoFilters\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.filters {
	@include section-spacing(calc($spacing-3xl + $spacing-1xl));
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\widgets\videoFilters\VideoFilters.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';
import { FilterPanel } from '@/features';
import Title from '@/shared/ui/Title/Title';
import VideoListSkeleton from '@/entities/video/ui/VideoList/VideoListSkeleton';
import { VideoList } from '@/entities/video';
import { useState } from 'react';
import { useFilteredVideos } from '@/shared/hooks/useFilteredVideos';
import { useVideos } from '@/shared/hooks/useVideos';

const VideoFilters = () => {
	const [filters, setFilters] = useState({ genre: '', country: '' });

	const filteredVideos = useFilteredVideos(filters);
	const defaultVideos = useVideos();

	const { items, isLoading } =
		filters.genre || filters.country ? filteredVideos : defaultVideos;

	const handleFiltersChange = (newFilters: {
		genre: string;
		country: string;
	}) => {
		setFilters(newFilters);
	};

	if (isLoading) return <VideoListSkeleton count={8} />;

	return (
		<section className={styles.filters}>
			<Title>Фильмы по категориям</Title>
			<FilterPanel onFiltersChange={handleFiltersChange} />
			<VideoList limit={8} items={items} />
		</section>
	);
};

export default VideoFilters;

────────────────────────────────────────────────────────────────────────────────

FILE: src\widgets\videoFrames\ui\FrameList\FrameList.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';
import type { Frame } from '@/entities/video/model/types';
import Image from '@/shared/ui/Image/Image';
interface Props {
	items: Frame[];
}

const FrameList = ({ items }: Props) => {
	return (
		<ul className={styles.list}>
			{items.map(({ imageUrl, previewUrl }) => (
				<li key={imageUrl} className={styles.frame}>
					{
						<Image
							src={imageUrl || previewUrl}
							alt={'Кадры фильма'}
							type='frame'
						/>
					}
				</li>
			))}
		</ul>
	);
};

export default FrameList;

────────────────────────────────────────────────────────────────────────────────

FILE: src\widgets\videoFrames\ui\FrameList\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.list {
	display: grid;
	grid-template-columns: repeat(3, minmax(300px, 400px));
	gap: $spacing-md;
	justify-content: center;
	width: 100%;
}

.frame {
	width: 100%;
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\widgets\videoFrames\ui\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.frames {
	display: flex;
	flex-direction: column;
	@include section-spacing(calc($spacing-3xl + $spacing-lg));
}

.noframes {
	text-align: center;
}

.title {
	display: inline-flex;
	align-self: center;
	padding: $spacing-2sm calc($spacing-2xl + $spacing-md);
	margin-bottom: $spacing-3xl;
	color: $color-secondary;
	background-color: $color-primary;
	border-radius: $radius-lg;
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\widgets\videoFrames\ui\VideoFrames.tsx
────────────────────────────────────────────────────────────────────────────────

import styles from './styles.module.scss';
import FrameList from './FrameList/FrameList';
import { Breadcrumbs } from '@/features';
import Title from '@/shared/ui/Title/Title';
import { useFrames } from '@/shared/hooks/useFrames';
import VideoListSkeleton from '@/entities/video/ui/VideoList/VideoListSkeleton';
interface Props {
	filmId: string;
}

const VideoFrames = ({ filmId }: Props) => {
	const { items, isLoading } = useFrames(filmId);

	if (isLoading) return <VideoListSkeleton count={6} />;

	const frames = items ?? [];

	if (frames.length === 0)
		return <h3 className={styles.noframes}>Доступных кадров нет(</h3>;

	return (
		<section className={styles.frames}>
			<Title>Кадры из фильма</Title>
			<Breadcrumbs to={'/'} direction='next'>
				Смотреть все
			</Breadcrumbs>
			<FrameList items={frames.slice(0, 6)} />
		</section>
	);
};

export default VideoFrames;

────────────────────────────────────────────────────────────────────────────────

FILE: src\widgets\videoSimilar\styles.module.scss
────────────────────────────────────────────────────────────────────────────────

.similar {
	display: flex;
	flex-direction: column;
	gap: $spacing-1xl;
}

.title {
	display: inline-flex;
	align-self: center;
	padding: $spacing-2sm $spacing-xl;
	color: $color-secondary;
	background-color: $color-primary;
	border-radius: $radius-lg;
	text-transform: capitalize;
}

────────────────────────────────────────────────────────────────────────────────

FILE: src\widgets\videoSimilar\VideoSimilar.tsx
────────────────────────────────────────────────────────────────────────────────

import Title from '@/shared/ui/Title/Title';
import { useSimilar } from '@/shared/hooks/useSimilar';
import VideoListSkeleton from '@/entities/video/ui/VideoList/VideoListSkeleton';
import { VideoList } from '@/entities/video';
interface Props {
	filmId: string;
}

const VideoSimilar = ({ filmId }: Props) => {
	const { items, isLoading } = useSimilar(filmId);

	if (isLoading) return <VideoListSkeleton count={4} />;
	if (!items.length) return null;

	return (
		<section>
			<Title>Возможно, вам понравится</Title>
			<VideoList items={items} limit={4} />
		</section>
	);
};

export default VideoSimilar;

────────────────────────────────────────────────────────────────────────────────

FILE: tsconfig.app.json
────────────────────────────────────────────────────────────────────────────────

{
	"compilerOptions": {
		"tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",
		"target": "ES2022",
		"useDefineForClassFields": true,
		"lib": ["ES2022", "DOM", "DOM.Iterable"],
		"module": "ESNext",
		"types": ["vite/client"],
		"skipLibCheck": true,

		
		"moduleResolution": "bundler",
		"allowImportingTsExtensions": true,
		"verbatimModuleSyntax": true,
		"moduleDetection": "force",
		"noEmit": true,
		"jsx": "react-jsx",

		
		"strict": true,
		"noUnusedLocals": true,
		"noUnusedParameters": true,
		"erasableSyntaxOnly": true,
		"noFallthroughCasesInSwitch": true,
		"noUncheckedSideEffectImports": true,

		
		"baseUrl": ".",
		"paths": {
			"@/*": ["./src/*"],
			"@app/*": ["./src/app"],
			"@pages/*": ["./src/pages/*"],
			"@widgets/*": ["./src/widgets/*"],
			"@features/*": ["./src/features/*"],
			"@entities/*": ["./src/entities/*"],
			"@shared/*": ["./src/shared/*"]
		}
	},
	"include": ["src"]
}

────────────────────────────────────────────────────────────────────────────────

FILE: tsconfig.json
────────────────────────────────────────────────────────────────────────────────

{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}

────────────────────────────────────────────────────────────────────────────────

FILE: tsconfig.node.json
────────────────────────────────────────────────────────────────────────────────

{
	"compilerOptions": {
		"tsBuildInfoFile": "./node_modules/.tmp/tsconfig.node.tsbuildinfo",
		"target": "ES2023",
		"lib": ["ES2023"],
		"module": "ESNext",
		"types": ["node"],
		"skipLibCheck": true,

		
		"moduleResolution": "bundler",
		"allowImportingTsExtensions": true,
		"verbatimModuleSyntax": true,
		"moduleDetection": "force",
		"noEmit": true,

		
		"strict": true,
		"noUnusedLocals": true,
		"noUnusedParameters": true,
		"erasableSyntaxOnly": true,
		"noFallthroughCasesInSwitch": true,
		"noUncheckedSideEffectImports": true
	},
	"include": ["vite.config.ts"]
}

────────────────────────────────────────────────────────────────────────────────

FILE: vite.config.ts
────────────────────────────────────────────────────────────────────────────────

import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
	plugins: [react()],
	resolve: {
		alias: {
			'@': path.resolve(__dirname, 'src'),
			'@app': path.resolve(__dirname, 'src/app'),
			'@pages': path.resolve(__dirname, 'src/pages'),
			'@widgets': path.resolve(__dirname, 'src/widgets'),
			'@features': path.resolve(__dirname, 'src/features'),
			'@entities': path.resolve(__dirname, 'src/entities'),
			'@shared': path.resolve(__dirname, 'src/shared'),
		},
	},
	css: {
		preprocessorOptions: {
			scss: {
				additionalData: `
					@use "@/shared/styles/utils/variables" as *;
					@use "@/shared/styles/utils/mixins" as *;
				`,
			},
		},
	},
});

────────────────────────────────────────────────────────────────────────────────

================================================================================
# END OF PROJECT CONTENT
